# 執行結果截圖
result.png
*.png

# 診斷暫存檔
!README.txt

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code 本地設定（個人權限，不分享）
.claude/settings.local.json

# Selenium 測試 log
selenium_login_test.log
chromedriver.log
chrome_browser.log
run.log
run.log.*
debug.log
final_state.png

# 帳密設定（PIN／校網帳密，嚴禁推上 GitHub）
env.env
id.txt

# install.py 記下「這台機器要用哪一支 Python」（每台不一樣，不該進版控）
python-path.txt

# 審核表與任何試算表（含公文主旨／摘要／承辦人親筆擬辦，嚴禁推上 GitHub）
# 這個 repo 是 public fork，一旦 commit 過就算之後刪掉也還留在 git 歷史裡，
# 等於永久公開。預設全擋，之後真要納管某個 xlsx 再用 ! 開例外。
*.xlsx
*.xlsm
*.xls
*.csv
公告彙整.xlsx

#目錄
document_download/*
document_download_closure/*
#強制 Git 不要忽略 .gitkeep 檔案（這樣目錄就會被保留）
!/document_download/.gitkeep
!/document_download_closure/.gitkeep

# doc_classifier
doc_classifier/training_data/*
!doc_classifier/training_data/.gitkeep
doc_classifier/runs.log
doc_classifier/runs.log.*
