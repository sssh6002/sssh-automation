# 安裝說明（給接手的人）

這份是**給不寫程式的人看的**。照著做就好，看不懂的地方不用管。
（工程細節在 `README.md`，那份是原作者的完整文件。）

---

## 這套工具在做什麼

把「收公文 → 摘要 → 擬辦 → 陳核 → 存查 → 公告文案 → 貼校網」這一條，
**粗工交給電腦、判斷留給你**。

它**不會**自己決定要不要送、要不要貼。每一個會送出去的動作都是你按下去、
而且會再問你一次。

---

## 一、先確認這四樣（電腦本身的東西，程式裝不了）

| 要有 | 為什麼 |
|---|---|
| **Windows** | 要用讀卡機與公文系統的 KdApp 元件 |
| **Google Chrome** | 程式靠它操作 edoc 與校網 |
| **讀卡機 ＋ 自然人憑證卡 ＋ HiCOS 元件** | 登入 edoc、簽章都要插卡 |
| **Python 3.10 以上** | 程式本體。**沒有的話 `安裝.bat` 會問你要不要幫你裝**（用 Windows 內建的 winget，不用管理員權限）；要自己裝就到 python.org，第一頁**記得勾「Add python.exe to PATH」** |

⚠️ **執行期間螢幕不能鎖** —— Windows 鎖屏會擋住讀卡，程式會卡在登入。

---

## 二、安裝

### 要裝在哪一顆磁碟？隨你 —— **但路徑不要有中文**

這套**沒有傳統的安裝程式，資料夾本身就是程式**。解壓縮到哪裡，它就裝在哪裡——
`D:\`、`E:\`、隨身碟都可以。下載的公文、設定檔全部待在那個資料夾裡面。

> ⚠️ **資料夾名稱請用英文或數字。**
> `D:\sssh-tool` ✅ ／ `D:\自動辦文工具` ⚠️
>
> 為什麼:公文系統那個「要存到哪裡」的對話框不是網頁，程式是**模擬鍵盤把路徑打進去**
> 的，而那個方式打不出中文。2026-08-14 有人裝在
> `D:\D_資訊系統\自動辦文工具\`，中文被吃掉，公文全部掉到 `D:\D_\` 裡面，
> **而且畫面上什麼都沒說**。
>
> 程式現在改成中文路徑時用「複製貼上」繞過去，多半會正常；但那條路也可能被擋
> （遠端桌面、剪貼簿工具）。**用英文路徑是零風險的，中文路徑是「應該可以」。**
> 已經裝在中文路徑了想搬:整個資料夾搬到英文路徑，再點一次 `安裝.bat`。

> **已經裝好之後想搬家？** 整個資料夾搬過去就好，但**搬完要再點一次 `安裝.bat`** ——
> 桌面捷徑記的是舊路徑。

⚠️ **唯一不在那個資料夾裡的是「公告彙整.xlsx」**（審核表），它預設在**桌面**。
想讓它跟公文放在同一顆磁碟，到「設定」頁的「審核表放哪裡」填完整路徑
（例：`D:\公文\公告彙整.xlsx`），然後**自己把舊的那份複製過去**——程式不會幫你搬。

### 開始裝

在這個資料夾裡，**點兩下 `安裝.bat`**。

它會做四件事，然後把「還缺什麼」用中文一次講完：

1. 找一支可以用的 Python，**記在 `python-path.txt`**
   （這台機器可能有兩個 Python，只有一支裝了寫 Excel 的套件。記下來以後就不會用錯。）
2. 裝相依套件
3. 沒有 `env.env` 的話，從 `env_example.env` 複製一份
4. **在桌面放一個「自動辦文工具」捷徑**
5. 跑環境檢查（`doctor.py`）

看到 **`都好了 ✅`** 就成功了。看到 `❌` 就照它下面那行「→」做，做完再點一次 `安裝.bat`。

---

## 三、填自己的帳密（**這一步不能跳**）

點兩下 **`啟動.bat`**，瀏覽器會自己打開介面。點右上角的「**設定**」分頁：

- **個人的**：自然人憑證 PIN、校網帳號、校網密碼——**就這三格**。
  這幾格是**你自己的**，前一個人的不會留下來，也不該留下來。
  畫面上永遠看不到現在填的內容，只顯示「已填／未填」。
  > 底下摺起來那個「**AI 服務的金鑰**」**不用管，空著才是正常的**。
  > （那是向外面的 AI 服務借額度用的密碼；這套工具預設用這台電腦上 claude 的
  > 登入寫摘要，不跟任何人借額度。）
- **共用的**：發布者、發布單位、摘要要叫哪些 AI
  → 這些是「這個學校、這個職務」的設定，通常照用就好。

⚠️ **PIN 打錯會鎖卡。** 自然人憑證連續輸錯要跑戶政事務所解卡，所以那一格存檔前會
再問你一次。程式在跑的時候**只填一次、絕不重試**，就是為了這件事。

⚠️ `env.env` 這個檔案裡有你的 PIN 與密碼。**絕對不要把它傳給任何人、不要上傳、
不要放進雲端硬碟。** 程式已經設定成不會把它送上 GitHub。

---

## 四、每天怎麼用

**點兩下桌面的「自動辦文工具」**（安裝時會自動放上去；也可以用資料夾裡的
`啟動.bat`，兩個是同一件事）—— 不用開任何開發工具、不用會寫程式。會發生兩件事：

1. 跳出一個**黑色視窗**
2. 瀏覽器自動打開介面（沒自動開的話，手動輸入 `http://127.0.0.1:8760`）

> ⚠️ **那個黑色視窗就是程式本體，不要關掉。**
> 關掉它網頁那一頁就跟著死了，而畫面上不會有任何提示——你會以為是網頁壞了。
> 收工時關掉它就好，下次再點一次 `啟動.bat`。
>
> 這個介面**只有這台電腦連得到**，別人連不進來。

然後從上面的分頁順著走：

| 分頁 | 做什麼 | 會送出東西嗎 |
|---|---|---|
| **摘要** | 看公文、改擬辦（改完自動存） | 不會 |
| **陳核** | 勾要送的，按送出 → 填擬辦、按陳會、簽章 | **會**（送出後收不回來） |
| **存查** | 讀待結案清單，勾要歸檔的 | **會**（歸檔後要找 admin 才救得回） |
| **公告** | 叫 AI 產公告文案，可以直接改字 | 不會 |
| **張貼** | 勾要貼的，貼上校網 | **會**（全校師生家長看得到） |
| **舊文** | 辦完的公文 | 不會 |
| **設定** | 帳密、判斷規則 | 不會 |

**會送出東西的那三頁都是紅底**，而且按下去都會再問一次、讓你看最後一遍要送的字。
不會送出的那幾頁是綠底。

三件程式幫不了你的事：

1. **執行期間不要動滑鼠鍵盤** —— 程式正在操作 Chrome。右下角會有小浮窗告訴你
   跑到哪，**它會明講「可以動滑鼠了」**，不要靠「視窗不見了」猜。
2. **審核表（桌面的「公告彙整.xlsx」）用 Excel 開著時，什麼都存不進去。** 先關掉。
3. 收文前先存好網頁 —— 收文會關掉自動化用的 Chrome。

---

## 五、出問題時

**先點一次 `安裝.bat`**，它會重跑環境檢查，把「現在缺什麼」講出來。九成的問題
（套件不見了、Excel 開著、設定沒填、規格檔改壞了）它都講得出來。

還是不行的話，這幾份給接手的人／資訊人員看：

- `HANDOFF_LATEST.md` —— **最重要的一份**。這套工具踩過的坑與為什麼這樣設計，
  都在裡面。接手第一件事就是讀它。
- `README.md` —— 原作者的完整技術文件。
- `doctor.py` 的輸出 —— 直接複製整段。它不會印出你的 PIN 與密碼，可以放心貼。

---

## 六、要把這套交給別人的時候

**不要只複製一個 `安裝.bat`** —— 那個檔只是開關，本體是旁邊那一百多個檔案。
（單獨拿一個 bat 去點，只會看到 `can't open file install.py`。）

在這個資料夾裡跑：

```
python handover.py
```

它會在**桌面**產生兩個東西（內容一樣，挑一個給人就好）：

- `自動辦文工具_交接_日期.zip` —— 一般的壓縮檔
- `自動辦文工具_交接_日期.exe` —— **點兩下就能解開**（自解壓縮檔），
  對方不需要會用解壓縮軟體。它**只解壓縮**，不會自己安裝任何東西 ——
  解出來還是要點「安裝.bat」。（需要這台電腦有裝 7-Zip 才產得出來。）

那兩個檔 **刻意不含**：

- `env.env`（你的自然人憑證 PIN、校網帳密）
- 公文內容（`document_download*/`）與審核表（`*.xlsx`）
- 這台機器的私事（`python-path.txt`、log、個人設定）

打包完它會**把 zip 打開再檢查一次**：混進不該有的就直接刪掉那個 zip；少了必要的檔
（例如 `安裝.bat`）也一樣刪掉重來。**寧可包不出來，也不要把 PIN 或公文送出去。**

給對方三句話就夠了：

1. 解壓縮到任何位置
2. 點兩下「安裝.bat」
3. 點兩下「啟動.bat」，到「設定」頁填自己的 PIN 與校網帳密

---

## 七、判斷規則要改的時候

「哪些公文不要自動送陳核」「哪一類公文歸哪個存查分類」這些判斷**不在程式裡**，
在「設定」頁下面那兩個表單：

- **他人業務判斷** —— 平常收起來，很少要改
- **存查分類對應表** —— 遇到新類型的公文（例如書展）就在這裡加一列，
  **愈上面的優先**

改完按「儲存設定」。存檔前程式會自己驗一次，**驗不過就整批不存**（不會存出一個
「看起來好了、其實讀不到」的檔案）。
