# sssh-automation 交接檔

> 每輪覆寫最新狀態。接手者先讀本檔，再看 `git log` 與 `git diff`。
> 最後更新：2026-09-03

## 這個 fork 在做什麼

承辦人（番茄，松山高中 MIS）fork 系管師的公文自動化，改成**半自動**：
系統只做兩頭粗工，判斷與送出由人決定。原因是他有**代理公文**、陳核路徑因人而異，
系統無法可靠判斷；且擬辦想自己掌控，但校網張貼繁瑣。

> 這份代理**沒有期限**，他自己也不知道要代理多久。所以：規則一律寫進規格檔而不是
> 靠人記，讓整套隨時能交給下一個人；也不要規劃「等代理結束再怎樣」的長線工程。

## 目前進度（2026-08-12）

**張貼頁 8/12 做完並實跑成功，六個分頁全部到齊，整條流程走完了。**
實跑 1 筆（`MWAA1156007964`）:置頂在第一筆、標題與五個內容區塊都對、報名網址正確、
沒有誤傳附件也沒有重複標題、本地標記／CSV 清冊／審核表／校網公開頁四邊一致。
測試 471 條全過。

**當天共貼出 7 筆（1 + 2 + 4），校網上的「單位」欄現在全部正確。**
中間抓到一個會貼錯單位的 bug 並修掉:「發布單位」那一欄程式回報選好了、實際沒設
進去，於是那筆被校網掛成「圖書館」。修法是設完**讀回來確認** → 不對就自己點下拉
的候選、每點一次就驗 → 還是不對就**這一筆不發佈**（詳見待補第一條 ——
那個欄位的長相很反直覺，值得先讀）。

**那道護欄兩邊都實跑驗證過了**:先擋住錯的（4 筆全擋、0/4、結束碼 1、一筆都沒貼），
修好之後放行對的（4/4 貼出去、單位全部是資訊媒體組）。

當天審出來並修掉的三道護欄（**全部都是「程式回報成功、其實沒做到」**那一類，
這個 fork 到目前為止最常見的坑型）:

1. **貼失敗卻用結束碼 0 離開** → UI 會寫「張貼結束」還自動收起面板（坑 #23）。
2. **送出前只比文號、不比內容** → 文案被換掉照樣貼得出去（坑 #24）。
3. **發布單位回報選好了、實際沒設進去** → 公告被掛成圖書館（坑 #25）。

做這一頁時查出一件會出事的事，已擋掉（坑 #22）:**張貼對 Chrome 的要求跟陳核／
存查正好相反**。那兩頁是 attach 收文開好的 edoc Chrome，要它開著;張貼不碰 edoc，
是自己開一個新的 —— 而兩邊用**同一個 Selenium 設定檔＋同一個 9222 埠**。
所以張貼要那個 Chrome **關掉**，兩頁的綠燈不能共用（共用一定有一頁是錯的）。

## 前一輪進度（2026-08-10）

**收文 → 摘要 → 擬辦 → 陳核，整條都實跑通了**（8/5–8/6 共送出 5 筆，
每筆都走完填字 → 存檔 → 按陳會 → PIN 簽章，磁碟有 `*已陳核.txt` 為證）。
**存查頁 8/10 做完並實跑通過** —— `MWAA1156007696`、`MWAA1156007710` 兩筆
都歸到檔號 03750401，`待結案 = 0,全部處理完畢(共跑了 2 輪)`，結束碼 0，
磁碟有 `已存查.txt` 為證。而且 log 上如願印出
`[archive_batch] 略過自動貼校網（這條路只歸檔）` ——
**歸檔與貼校網真的拆開了**，7/28 那種「歸完檔自動送上校網」不會再發生。

那天第一次跑其實失敗過一次（第 1 輪就中止、一份都沒歸檔），原因見坑 #17：
殘留的公文閱覽器分頁害 `document_closure` 對到別份公文。清掉分頁後重跑就過了，
順帶說明第一次那個「匯出公文資料對話框 10 秒沒關」也是同一件事的連鎖反應
（第二次沒再出現）。

**公告頁 8/10 同日做完**（離線、只產文案，沒有實跑風險，測試 14 條全過）。
這一頁**不碰 edoc、不用 Chrome、不用插卡** —— 唯一會弄壞東西的動作是「重產」
（會蓋掉承辦人自己改過的字），所以那顆是逐筆＋二段確認。

**收文 → 摘要 → 擬辦 → 陳核 → 存查 → 公告，整條打通。** 只剩「張貼」一頁。

| 階段 | 工具 | 狀態 |
|------|------|------|
| 備料（收文＋LLM 摘要） | **`prep_batch.py`**（fork，拆兩段） | ✅ 要讀卡機。UI 的「收新公文」走這支 |
| 備料（原作者那條） | `main.py 4` → `document_system.process_document_prep` | ✅ 沒改，但**會被逾期踢**（坑 #19） |
| 審核表 | `review_sheet.py`（桌面「公告彙整.xlsx」） | ✅ |
| 本機介面 | `ui.py` + `ui_page.html`（127.0.0.1:8760） | ✅ 摘要／舊文／陳核／存查 |
| 跑文進度浮窗 | `prep_hud.py` | ✅ 右下角置頂，`--job=send`／`--job=archive` |
| 公告文案 | `announce_doc.py`（離線） | ✅ 含 `--sent-only`，UI 公告頁已接上 |
| 貼擬辦送陳核 | `post_draft_batch.py` | ✅ **8/6 實跑 5 筆全部成功** |
| 結案存查（只歸檔） | `archive_batch.py` | ✅ **8/10 實跑 2 筆全部成功** |
| 貼校網（fork） | **`post_web_batch.py`**（貼公告欄那份文案） | ✅ **8/12 實跑 1 筆成功**（發布單位要手動補，見待補） |
| 貼校網（原作者） | `post_web_review.py`（`main.py 5`） | ⚠️ 尚未實跑;**貼的是總結.md，不是公告欄** |
| 找舊文 | **`find_doc.py`**（唯讀，掃 `D:-公文`） | ✅ **2026-08-26 上線**，1492 筆，比對資料夾名 |
| 歸檔到檔案櫃 | **`file_doc.py`** ＋ `file_doc.md` | ✅ **2026-08-31 實跑 27 筆全部成功** |

**UI 分頁**：摘要 ✅、舊文 ✅、陳核 ✅、存查 ✅、公告 ✅、張貼 ✅、
找舊文 ✅（2026-08-26）、設定 ✅。

## 找舊文怎麼運作（2026-08-26 新增）

承辦人要的是「**用關鍵字找舊文**」。這一頁跟其他頁都不同:**它不碰 edoc、不碰
Chrome、不碰工作區，只讀承辦人自己的檔案櫃**（`D:-公文`，1492 筆，109～115 年）。
全站唯一沒有確認關卡的頁面 —— 因為它沒有任何收不回來的動作，能按的只有「開資料夾」。

**為什麼只比資料夾名，不搜 PDF 內文。** 櫃子裡每一筆只有 PDF（來文／合併版／
opinion），沒有純文字檔。要搜內文得先把一千多份 PDF 抽字建索引 —— 那是另一批工程，
而且掃描檔抽不出字。而資料夾名本身就帶了承辦人歸檔時寫下的四樣東西:
日期、文號、【標籤】、精簡標題，平常想得起來的關鍵字都在裡面。

**名字有五六種寫法，解析一律盡量猜、猜不到也不擋。** 早期沒底線
（`1121003MWAA1126009285清華…`）、沒【標籤】（改用 `_` 接標題）、
甚至有 `1120MWAA1156000609` 這種日期只剩 4 碼的。
關鍵是:**解析結果只決定畫面上分成哪幾欄顯示，真正拿去比對的是原始資料夾名**，
所以名字再怪都找得到。改 `parse_name` 不會讓任何一筆變成搜不到。

搜尋規則（`find_doc.search`，畫面上的 JS 必須跟它一致）:
關鍵字用空白分開、**每個都要出現**才算命中（AND）、大小寫不分、**空白不計**
（櫃子裡的標題常有空格如 `臺北市 113 年寒假 STEAM`，承辦人打字時不會照著空）。

**畫面上那個黃框是重點護欄。** 工作區有 26 筆還沒複製進櫃子，那幾筆在這一頁
**搜不到** —— 不講的話承辦人會以為公文不見了，跑去 edoc 重查甚至重新歸檔一份。
所以 `find_payload()` 每次都算一遍 `not_archived()`，畫面出黃框報數字。
櫃子整個不見（路徑錯、外接碟沒插）也要明講，不能默默給一張空清單。

## 歸檔到檔案櫃怎麼運作（2026-08-27 新增）

「找舊文」的另一半:**沒歸進櫃子的公文在那一頁搜不到**。入口在找舊文頁那個黃框
（「工作區還有 N 筆沒複製進櫃子」→ 看看要怎麼歸），執行器是 `file_doc.py`，
判斷全在 `file_doc.md`（已加進設定頁的規格檔清單）。

⚠️ **這個「歸檔」跟存查頁的「歸檔」是兩件事。** 存查頁是在 edoc 上結案
（無 admin 介入無法復原）;這一顆只在承辦人自己的硬碟上複製檔案，不碰 edoc、
不碰 Chrome、不用插卡。畫面上那段綠框就是在講這件事，不要拿掉。

**一定是複製，不是搬移。** 工作區的目錄名（`MWAA…`）是 `summarize_doc.py`／
`review_sheet.py`／`post_web_batch.py` 拿來找檔案的鍵 —— 搬走或改名，那幾支會
**安靜地**找不到東西（不噴錯，只是從此少處理一批）。`file_doc.py` 從頭到尾只有
`shutil.copy2`，測試裡每個會動檔案的案例都順便驗一次「工作區完全沒變」。

四道擋:
1. **還沒辦完的不歸** —— 沿用 `review_sheet.is_archived`，不另立標準。
2. **文號已在櫃子就不再歸** —— 比文號，不比名字（櫃子裡的名字他會自己改）。
3. **名字撞了不覆蓋** —— 目標資料夾已存在就整筆不做（`copy_one` 再擋一次）。
4. **痕跡檔不進櫃子** —— `已存查.txt`／`已公告.txt`／`.bak` 留在工作區。
   它們同時也長得像 `*.txt`，所以規則衝突時「不要複製」必須贏。
   沒列到的副檔名一律進 `來文`:寧可多帶一份，不要安靜弄丟。

**檔案怎麼擺**（`file_doc.md` 可改）:PDF／附件進 `來文/` 子資料夾（跟他櫃子裡
多數一致），`內容.txt` 與 `總結.*.md` 放根層 —— 那兩種是櫃子裡唯一的純文字檔，
**日後要做「搜 PDF 內文」就是靠它們省下抽字的工**。

**名字是草稿，不是定案。** `<民國建立日>_<文號>【標籤】<精簡標題>`;判不出標籤就
改用底線接標題（櫃子裡早期就是那種寫法）。標題砍法:先砍公文套語 → 有「」就取
引號裡那段（他櫃子裡多數名字就長那樣）→ 最後才截長度（順序不能換）。
畫面上標籤與標題兩格都能直接改，改完即時看到「會變成」什麼名字。

**判斷全在 `file_doc.md`。** 標籤對應表（愈上方愈優先、`OR` 分隔）、要砍的套語、
引號優先、長度上限、哪些檔案複製到哪 —— 承辦人改那份就好，不必動 Python。
規格檔讀不到時**回一份空規則**（標籤留白、標題不砍字），不自己編一套預設值。

**2026-08-31 實跑 27 筆，全部成功。** 櫃子從 1492 筆變 1519 筆，工作區
`已存查.txt` 那些痕跡檔留在原地沒被帶走，來源檔案數一個沒少（48+41 個目錄、
257+269 個檔，複製前後相同）。剩下 3 筆是「還沒辦完」，等流程走完自然會出現。

那一批有 5 筆自動命名砍歪（主旨太繞、引號套引號、超過長度上限），
是**手動指定標籤與標題**才歸的 —— 這就是那兩格設計成可以直接改的原因。
同一批也順手補了兩條規則進 `file_doc.md`:`挑戰賽` 算競賽、開頭的「為」要砍。
櫃子裡的資料夾名**不是任何程式的鍵**（比對一律用文號），事後在檔案總管改名沒問題。

## 存查怎麼走（2026-08-06 新增，**下一段的工作**）

⚠️ **不要用 `main.py 3`**。系管師那條路歸檔成功後會緊接著呼叫
`maybe_post_announcement` 自動貼校網，中間沒有任何人工確認 ——
2026-07-28 就是這樣把他人業務的內部公文送上校網。

承辦人 2026-08-06 決定**拆開**：歸檔可以自動，貼什麼上校網要自己決定
（走「張貼」那條 `post_web_review` 的逐筆確認）。

fork 側的執行器是 `archive_batch.py`：

```
python archive_batch.py         只預覽:待結案有哪幾筆、各自會歸到什麼檔號
python archive_batch.py --go    真的歸檔（無 admin 介入無法復原）
```

- 呼叫前把 `document_closure_post_web.maybe_post_announcement` 換成不做事的版本。
  原程式那行是**函式內 import**，換模組屬性就生效；`document_closure.py`
  一行沒改，系管師自己跑 `main.py 3` 完全不受影響。
- 同時把 `_get_sidebar_paren_count` 包一層（讀不到才退回 JS 版）——
  `process_document_closure` 每輪用它讀「待結案(N)」，選單收合時會讀不到
  而「無法判讀待結案數,中止迴圈」，跟坑 #5 同源。
- **檔號讀不到就擋下，絕不猜**。預覽會標明檔號是從結案目錄還是承辦中目錄讀的
  —— 兩份可能不一致（見待補項的 MWAA1156007051）。

離線判定已對真實的 13 個結案目錄跑過：5 筆可歸檔、1 筆缺檔號擋下、7 筆已存查。

## 存查頁怎麼運作（2026-08-10 新增）

跟陳核頁**不是同一個形狀**，兩個根本差別:

**1. 順序是資訊，不是排版。** `process_document_closure` 每輪只處理待結案清單的
**第一筆**，成功才進下一輪；任何一筆過不了就 `return False` 整批中止（讀不到
8 位檔號正是這種）。所以「這一筆過不過得了」跟「這一批做不做得到它」是兩件事：
被擋那筆**後面**的公文判定再乾淨，這批也輪不到。頁面因此照 edoc 的順序排、
每筆掛序號，停止點畫一條虛線「以下這批做不到」，而輪不到的那幾筆要明講
「它本身沒問題，是被前面拖累」—— 不然承辦人會跑去查一份好好的公文。
`archive_batch.plan()` 算這件事，每筆掛 `狀態`：`go`／`stop`／`after`／`danger`。

⚠️ 還有一種停法**預覽看不出來**：主管還沒核決「如擬」的公文會被跳過、清單順序
不變，連兩輪之後整批停下（`_NO_APPROVAL_STREAK_LIMIT`）。那是 edoc 上的狀態，
磁碟沒痕跡。頁面上有寫這件事，不要讓人以為是程式壞了。

**2. 清單要去 edoc 讀。** 陳核的候選算得出來（審核表＋磁碟），待結案清單只存在
edoc 上。所以這一頁**打開不會自動算** —— 「算」就是去點 edoc 的選單。要按
「讀待結案清單」（跑 `archive_batch.py --json`，只切頁面不歸檔），讀完才有東西
可以確認。四支會動 Chrome 的工作（收文／送陳核／讀清單／歸檔）走**同一把**
`ui.Job` 互斥鎖。

其餘照陳核頁：兩段式確認、`ui.chrome_state` 事前檢查、送出走 subprocess。
`/api/archive/start` 三道關卡：沒帶「確認」不動；**整份清單含順序**要跟剛讀到的
一樣；`plan()` 說不可跑就不放行。放行後把 `--expect=<整份清單>` 帶下去，
`archive_batch` 動手前**自己再讀一次 edoc 清單**比對 —— 介面那道只擋得住畫面過期。
歸檔一開跑就把快取的清單作廢（`SCAN.lines = []`），免得有人拿過期清單再按一次。

**3. 跑完要留下痕跡（2026-08-31 新增）。** 歸檔一開跑清單就作廢，所以跑完這一頁會
退回「還沒讀過清單」——**跟從來沒跑過長得一模一樣**；而進度面板那句「歸檔結束」
3 秒後自己收起來，收掉就什麼都不剩。承辦人的回報是「存查完沒有訊息告知已經存查
完畢」：一個無 admin 介入救不回來的動作，做完了畫面上一點交代都沒有。
現在 `/api/archive/plan` 多回一個 `上批`（`ui.archive_result()`），存查頁最上面留一個
結果框，講「送出幾筆、完成哪幾筆、哪幾筆沒有存查完成的痕跡」，留到下次按
「讀待結案清單」為止（`/api/scan/start` 會清掉）。

⚠️ 「真的歸檔了」認的是**磁碟上的存查標記檔**（`ab.evaluate()` 的 `已存查標記`），
不是結束碼、也不解析輸出文字。結束碼 0 不代表歸檔成功（跟坑 #23 同一型），
而標記檔正是舊文頁與 `plan()` 的 danger 判定認的**同一個**痕跡 —— 這裡另立一套
標準的話，最壞的分岔方向是「畫面說歸檔了、其實沒有」。

判定完全交給 `archive_batch.plan()`，介面不另立標準（同陳核頁的理由）。
`tests/test_ui_archive.py` 釘住這幾道關卡。

## 收新公文怎麼走（2026-08-10 新增，`prep_batch.py`）

```
python prep_batch.py                  兩段都跑（UI 的「收新公文」走這支）
python prep_batch.py --attach         接已經登入好的 Chrome，不重登（登入卡住時用）
python prep_batch.py --download-only  只下載，不叫 AI
python prep_batch.py --summary-only   只補摘要，**完全不碰 edoc、不用插卡**
```

**2026-08-11 實跑成功:5/5 筆。** 第一段（下載 3 筆）只花 **55 秒**，第二段
（補摘要 3 份）花 6 分 7 秒 —— 而那 6 分鐘完全不碰 edoc，**全程沒有逾期**。
對照 8/10 沒拆之前:進清單到被踢中間 4 分 50 秒，其中 3 分 42 秒是乾等 AI。
昨天被吃掉的 `MWAA1156007934`、`MWAA1156007936` 都補回來了。

**第一段只下載、第二段離線補摘要。** 理由見坑 #19:邊下載邊叫 AI 的話，
edoc 在等 AI 的 1～2.5 分鐘裡完全沒人碰，閒置到逾期就把後面的公文吃掉。
拆開後第一段每筆之間只隔十幾秒，第二段完全不碰 edoc、**可以動滑鼠**
（程式會明講，坑 #13 的教訓）。

做法跟 `archive_batch.py` 同一招:呼叫前把 `summarize_doc.summarize_extracted`
換成不做事的版本（`pending_doc_handler` 那行是**函式內 import**，換模組屬性就
生效）。`document_system.py`、`pending_doc_handler.py` **一行沒改**。
覆寫包在 `contextmanager` 裡、**try/finally 還原** —— 沒還原的話第二段會靜靜
什麼都不產，然後印「補摘要完成 0 份」，看起來像「本來就沒事要做」。

⚠️ **第二段只補「這次收到的」，不補歷史公文。** `document_download/` 裡躺著
一堆早期跑到一半留下的空殼（2026-08-10 實測 9 個 006xxx 的目錄缺總結，旁邊
還有沒刪的 `.zip`）。第二段若直接補「所有缺總結的」，按一次「收新公文」就是
九次 token、十幾分鐘，而且多半是早就辦完的公文 —— 那不是按下那顆鈕的意思。
判斷用目錄 mtime（`touched_since`），**而且往前退 2 秒當安全邊際**:mtime 的
解析度可能只到秒，同一秒收到的公文會因為截斷而被誤判成歷史公文。
兩種錯的代價不對稱 —— 漏掉 = 公文靜靜沒摘要（要人工發現），多算 = 多花一次
token，所以往「寧可多補」退。跑完會**講出來**還有幾份舊的沒補，但不擅自去補。

要補歷史的請明確跑 `--summary-only`（那條補全部）。
`tests/test_prep_batch.py` 釘住這幾條（17 條）。

### 中文安裝路徑會把公文下載到別的地方（2026-08-14 新增，坑 #27）

同事把工具裝在 `D:\D_資訊系統\自動辦文工具\`，跑收文，**公文全掉到
`D:\D_\document_download`**，而畫面上什麼都沒說 —— 又是這個 fork 最愛的坑型:
**有人回報成功，但沒有人回頭確認那是真的。**

根因在 `pending_doc_handler._send_text_vk`（系管師的，**沒改**）。KdApp 的
「匯出公文資料」是 Java Swing 的檔案對話框，**不是網頁**，Selenium 碰不到，所以
那支是用**模擬實體鍵盤**把路徑打進去的 —— 逐字丟給 `VkKeyScanW`，而那支只認得
ASCII。中文字拿到 `-1`，程式印一行 WARN 然後 **`continue` 跳過**:

```
D:\D_資訊系統\自動辦文工具\document_download   ← 程式想打的
D:\D_\document_download                        ← 實際打進去的
```

Windows 遇到不存在的路徑會**自己建**，所以對話框照樣關、程式照樣往下跑、
結束碼照樣 0。那支的註解自己寫著「這個路徑只用英文，夠用」—— 在承辦人的機器上
成立（`D:\sssh-automation` 全英文），在別人的機器上不成立。**經典的
「在我這台是好的」。**

修在 fork 側:`prep_batch.unicode_safe_dialog_path()`（跟 `download_only()`
同一招，換模組屬性、try/finally 還原）。

- 路徑**純 ASCII** → 原封不動走原本那條已經實跑驗過的鍵盤路。行為零變化。
- 路徑**有中文** → 改成「放進剪貼簿 + Ctrl+V」。Swing 對剪貼簿是 Unicode
  安全的，模擬鍵盤不是。放進去之後**再讀回來比對**才算成功（坑 #25 的教訓:
  自己回報成功不算成功）。
- 剪貼簿失敗 → **什麼都不打**。這裡很容易寫錯成「退回去用鍵盤打」，但那正是
  這次的病:打半截路徑 = 公文靜靜掉到別的資料夾。什麼都不打的話對話框不會關，
  原本那道「10s 內對話框沒關閉」就會叫出來 —— **寧可看得見的失敗。**

`doctor.py` 的 `check_path_ascii()` 在安裝時就會講一句（只警告不擋 —— 擋下來
就沒人收得到文了；剪貼簿那條路多半會通，只是遠端桌面／剪貼簿工具可能擋）。
`安裝說明.md` 那句「要裝在哪一顆磁碟？隨你」也改了 —— **是「隨你，但路徑不要有
中文」**，英文路徑零風險，中文路徑是「應該可以」。

⚠️ **這條只在英文路徑的機器上驗過（那條分支等於沒動）。剪貼簿那條分支的
單元測試過了，但沒有人在真的中文路徑上實跑過。** 要驗就是:裝在中文路徑、
跑一次收文、**去看「摘要」頁有沒有出現新公文**，而不是看程式有沒有報錯。

### 摘要全滅時，畫面長得像「公文沒收到」（2026-08-18 新增，坑 #28）

承辦人早上跑收文，edoc 那邊「承辦中(1)」有一筆，UI 摘要頁卻是 **0 筆**，
看起來像沒收到文。實際上**公文早就下載好了**（`document_download\MWAA1156008152`
裡 PDF、附件、合併版都在），死在第二段:**四個 LLM backend 全滅**。

沒有 `總結.md` → `review_sheet.sync()` 把那個目錄當「還沒備料」略過 → 審核表沒那列
→ 摘要頁 0 筆。log 最後那句「掃到 55 個目錄，**5 個還沒備料**」就是唯一線索，
但它長得像雜訊，而且畫面上完全看不出「檔案其實在磁碟上」。

當天四棒全倒的原因各不相同（值得記，因為只有第三棒是免費又不用金鑰的）:

| backend | 為什麼倒 |
|---------|----------|
| antigravity | 那個 Google 帳號**沒有 Antigravity 資格**（永久性錯誤，每份白等 10～13 秒） |
| aistudio | `env.env` 沒填 Gemini 金鑰 |
| claude | 本機 `claude` CLI 當下**沒登入** |
| anthropic | 沒填 API 金鑰 |

修了兩處，兩處都是「錯誤訊息在騙人」那一類:

1. **`doctor.py` 只檢查「`claude` 這個命令在不在」，沒檢查有沒有登入** ——
   所以那天早上體檢是綠的，實跑才炸。新增 `claude_login_state()` +
   `check_llm()`:**真的叫一次 `claude -p`**（很短、幾乎不花錢）。
   ⚠️ 判斷不能只看結束碼:沒登入時是 **rc=1、stderr 空的**，真正原因在 stdout 的
   JSON（`is_error=true`、`result="Not logged in · Please run /login"`）；
   而 `is_error=true` 也可能配 **rc=0**。兩邊都要讀。
   因為這一項會連線，開場那句「這支只讀不寫」補了一行例外說明 ——
   不然 doctor 自己就在騙人。
2. **`summarize_doc._llm_summarize_claude_code` 失敗時只印 stderr**，於是 log 上
   是一句 `claude -p rc=1,stderr=''` —— 什麼都沒講。改成優先讀 stdout JSON 的
   `result`，現在會直接印出「沒登入」。

`tests/test_doctor.py` 加 7 條釘住（含「rc=0 但 is_error」與「stdout 不是 JSON
不可以讓 doctor 掛掉」）。全套 560 條過。

**下次遇到「摘要頁 0 筆」先做這件事**:去 `document_download\<文號>` 看有沒有
`*總結.*.md`。有目錄沒總結 = **不用重收文**，跑 `python prep_batch.py --summary-only`
補就好（⚠️ 那條會補**全部**缺總結的，當天是 5 份；只要補一份就
`python -c "import review_sheet as rs; rs.prepare(only=[r'...\document_download\<文號>'])"`）。

當天結果:那筆補完摘要，主旨／摘要／擬辦三欄都進了審核表，UI 待辦回到 1 筆。順帶一提，`claude` 那一棒**現在是通的** ——
08:16 失敗、09 點多再叫就成功了，所以那次多半是憑證過期的短暫狀態，
不是設定壞掉。這也正是為什麼那道檢查要「真的叫一次」而不是看設定檔。

同日順手把 `env.env` 的 `summarize_llm_order` 從
`antigravity,aistudio,claude,anthropic` 改成 **`claude,aistudio,anthropic`** ——
antigravity 那一棒是**永久性**的資格錯誤（不是暫時的），留著只是讓每份公文
白等 10～13 秒。後面兩棒沒金鑰會**立刻**回 None，不花時間，留著當備胎。
（要改回去或換順序，走介面「設定」頁的「摘要要叫哪些 AI」，別手改檔案 ——
那一頁存檔前會驗過名字，而且逐行保留註解。）

## 紙本轉線上公文（2026-09-03 新增）

承辦中清單最左邊有個**「簽核」欄**:`線` = 線上簽核（一般電子公文），
`紙` = **紙本轉線上**。承辦人回報:這種幾乎 100% 是「附件為實體海報、內容請求
張貼海報」的公文。

流程是**人的流程，沒辦法自動化**:紙本送到承辦人手上 → 他自己掃描 →
在 edoc 勾那一列按上方「轉線上」→ 彈窗「是否導向開啟來文上傳功能？」按確定 →
在轉線上畫面把**來文本文 pdf 與附件分開上傳**（edoc 紅字明文要求，不可以掃成
同一個 pdf）→ 按「轉線上陳核」。要實體公文、要掃描器，機器插不上手。

於是它在兩個地方會出事，各擋一道:

**一、轉線上之前跑備料 —— 一定收不到。** 來文本文還沒掛上去，公文閱覽器裡
連「下載」按鈕都沒有，備料等 20 秒 timeout 然後
`[pending_doc_handler] 下載/解壓縮失敗`。2026-09-03 實測 `MWAA1156008767`。

**不擋它**（擋要改系管師那支點清單的函式），改成**事後講清楚**:
`list_pager.page_doc_nos` 讀清單時順手把每一列的「簽核」欄記進 `SIGN_KINDS`
（附在原本那次 column-index 掃描裡，不多跑 query;讀不到那一欄就照舊只讀公文號，
不能讓它害整個備料讀不到清單）。第一段結束後 `prep_batch.download_report()`
拿清單每一筆去**跟磁碟對帳**，收不到而簽核欄是「紙」的就明講原因與該做什麼。

**二、轉完線上之後 —— 主檔是掃描影像，摘要會瞎編。** `summarize_doc.py` 用
`pypdf` 抽字、不做 OCR。掃描件抽到 0 字（或掃描器附帶 OCR 撈到零星幾個字），
舊版只有 `if raw.strip():` 這一關 —— **有一個字就照送 LLM**，會生出一份讀起來
很順、內容全是瞎編的假總結。

現在主檔抽到的字數低於 `_MIN_MAIN_PDF_CHARS`（100 字）就寫
`…紙本轉線上需手動處理.txt` 標記檔、**直接 return 不送 AI**，審核表顯示黃色的
「紙本轉線上，需手動處理」（跟「含個資，未送 AI」同一套機制）。
基準:一般電子公文主檔抽得到上千字（實測 `MWAA1156008753` = 1547 字）。

順手修掉一個會騙人的訊息:以前「目錄裡沒有主檔」和「主檔抽不到字」都印同一句
`找不到主檔 PDF`——檔案明明躺在目錄裡卻說找不到。現在兩種分開報。

**想過但決定不做:掃描頁當圖片送看得懂圖的模型**（2026-09-04 承辦人拍板）。
實測那份掃描件 pypdf 抽 0 字、整頁影像卻清楚可讀，技術上做得出來。不做是因為:
這種公文一年不到 10 份（比代理還稀有，代理承辦人也是手動辦）;它本來就得看實體
海報才辦得了，摘要省不了事;而且會**繞過個資關卡** —— `scan_pii()` 掃的是抽出來
的文字，沒文字等於沒掃，但影像裡照樣有姓名電話 email。以後想改主意先解決這點。

門檻與標記檔文字在 `summarize_doc.py` 最上面的常數;規則說明寫在
`summarize_doc.md` 的 **HTML 註解**裡（`_strip_html_comments` 會在送 LLM 前
整段剝掉，給人看、不佔 prompt）。

## 公告頁怎麼運作（2026-08-10 新增）

**跟前兩頁的根本差別:這一頁不會送出任何東西。** 只叫 LLM 產文案、寫回審核表
「公告」欄。不碰 edoc、不用 Chrome、不用讀卡機，所以**沒有** Chrome 狀態檢查、
沒有紅底警語、進度面板也不寫「請不要動滑鼠」（那句話在這裡是假的，而假警告會
讓真警告一起被當成裝飾 —— 坑 #13 的教訓是「能不能動滑鼠要明講」）。
頁首改用綠底講清楚「這一頁不會貼上校網」。

分三堆，全部照 `announce_doc.plan()` 的答案畫，介面不另立標準:

- **還沒有文案** = `plan(overwrite=False)` 的 todo。每筆一顆「產這一筆」，
  底下一顆「產生這 N 筆」（只填空白的，永遠不會蓋掉現有文案）。
- **已有文案** = 公告欄有字的。整篇攤開、可以直接改，停 1 秒自動存回「公告」欄
  （跟摘要頁的擬辦同一個做法）。「能不能重產」= 它在不在
  `plan(overwrite=True)` 的 todo 裡 —— 自己判「有沒有打過 OK」會變成第二套標準。
- **不會產** = 其餘的 skip，按 `plan()` 給的原因原文收合分組。不能不列:
  承辦人常要確認「為什麼我那筆沒出現」。

⚠️ **唯一的破壞性動作是「重產」** —— 它會把現在那一篇整篇換掉，含承辦人自己
改過的字。所以重產是**逐筆**、要再確認一次，對話框裡直接寫「要留的話先按取消
再按複製全文」。批次那顆永遠不帶 `--overwrite`。
`/api/announce/start` 兩道關卡:沒帶「確認」不動；要產的那幾筆**用待會真的帶
下去的同一組旗標**再問一次 `plan()`，少一筆就整批退回並說明原因（不會默默少產
一份）。帶下去的是 `--only <文號…>`，**絕不用 `--limit`** —— 2026-07-28 那次
就是 `--overwrite --limit 1` 挑錯對象，把手寫的公告蓋掉三次。

**「只列已陳核的公文」預設是開的**（CLI 的 `--sent-only` 預設是關的）。理由:
還沒陳核的擬辦還會改，先產文案容易白做、白花 token。畫面上有勾選框可以關掉，
被濾掉的公文會出現在「不會產」並寫明原因，不會默默消失。

文案裡有 `announce_doc.STRAY_MARK`（來文找不到的網址）時，那一筆會多一條紅字
提醒。那個標記字串已拉成常數兩邊共用 —— 各寫一次的話，哪天改了文字，介面那個
提醒就會默默消失。

產文案**照樣走同一把 `ui.Job` 互斥鎖**，雖然它不碰 Chrome:它跟收文一樣會寫
桌面那份 xlsx，兩支同時 load→save 會互相蓋掉，先存的那邊直接消失。
`tests/test_ui_announce.py` 釘住這幾道關卡（14 條）。

## 「這筆我自己辦掉了」（2026-08-12 新增，陳核／公告／張貼三頁）

承辦人的要求:「手動處理的標示，不要再撈進來」。

**資料層本來就有** —— 審核表那兩欄是三態，不是勾選框:

| 欄位值 | 意思 |
|---|---|
| 空白 | 還沒審，等你決定 |
| `OK` | 請程式去做 |
| **`已辦`** | **我手動辦掉了，程式完全不要碰**（也吃「手動／不用／跳過／-」等寫法） |

四支執行器早就都認第三態，缺的一直是**按鈕**:三頁每列只有「加入／取消勾選」，
所以手動辦掉的公文會永遠躺在「其他待辦」裡積著。現在每列多一顆
「我自己辦掉了」，寫的是**那一頁對應的那一關**:

- 陳核頁 → 「陳會」欄
- 公告頁、張貼頁 → 「張貼」欄（文案與張貼是同一條軌，`announce_doc.plan()`
  看到那一欄已辦就整筆略過）
- **存查頁不做** —— 那頁的清單是去 edoc 讀的，真的手動存查掉它就不在待結案裡了。

⚠️ **標了之後那一筆會從畫面上消失，而它會去兩個不同的地方**（按下去的提示會講）:

- 兩道關卡都算辦完 → **整份結束，移到「舊文」**（那裡的「放回待辦」會清掉兩欄）
- 還有一關沒完 → 留在那一頁底下的「**你標成自己辦掉的**」區，可以放回

而 `_detect_done` 會**自動**把有 `已陳核.txt`／`已存查.txt` 的公文標成「陳會=已辦」，
所以走完正常流程、到得了張貼頁的公文，「陳會」欄幾乎一定已經是已辦 ——
**在張貼頁按這顆，那筆多半直接進舊文，自辦區在那一頁實務上常常是空的。**
那不是壞掉:「不用貼」對那份公文來說就是最後一關。反悔去舊文頁。
（2026-08-12 實測承辦人的審核表:8 筆可貼的公文，「陳會」欄全部都是已辦。）

`rs.marked_done(gate)` 算那一區，**排除已經整份辦完的** —— 不排的話那一區會長成
第二份「舊文」。`tests/test_ui_send.py`／`test_ui_web.py` 釘住這幾條。

## 陳核頁怎麼運作（2026-08-04 新增）

介面照 CLI 的設計走兩段，**沒有一鍵直送**：

1. 頁面分三堆：**會送出**（框起來的就是實際填進 edoc 的兩行字）、**勾了但送不出去**
   （附原因）、**其他待辦**（按「要送」= 在審核表「陳會」欄打 OK）。
2. 按「送出這 N 筆」→ 對話框再列一次每一筆要送的字 → 「確定送出」才動。

送出走 `subprocess post_draft_batch.py --go`，不在 ui.py 裡另開 Selenium ——
實跑的是那支已經有安全關卡（逐筆失敗即中止、成功寫 `*已陳核.txt` 不重送）的程式。

兩件事**不能同時跑**（會搶同一個 Chrome），`ui.Job` 互斥擋掉，訊息會講是誰在跑。

`/api/send/start` 兩道關卡：**沒帶「確認」一律不動**（誰誤打這個位址都不會送出東西）；
**畫面看到的那幾筆要跟現算的完全一樣**，中間有人動 Excel 或又收了新公文就退回去重看。

判定完全交給 `post_draft_batch.evaluate()`，介面不另立標準 ——
兩套規則遲早分岔，而最壞的方向是「畫面說會擋、其實送出去了」。
`tests/test_ui_send.py::test_page_and_cli_agree` 就是釘這件事。

## 張貼怎麼走（2026-08-11 執行器、2026-08-12 頁面）

### 張貼執行器（`post_web_batch.py`）

```
python post_web_batch.py                            只預覽，什麼都不貼
python post_web_batch.py --json                     印一行 JSON 給 UI 用
python post_web_batch.py --go --expect=<文號,文號…>  真的貼
```

⚠️ **最重要的發現:貼的內容一直是錯的。** 原作者的 `post_web_review`
（`main.py 5`）貼的是 `*總結*.md` 解析出來的「主旨＋數字條列」，
**跟公告頁產的文案完全是兩套** —— 承辦人在公告頁改了半天的文案，根本沒被拿去貼。
`post_web_batch` 改成貼**審核表「公告」欄**那份:呼叫 `maybe_post_announcement`
前把 `_parse_summary` 換成吃公告欄的版本（**只換 title/body**，承辦文字與同步
分類原樣保留 —— `_should_post` 跟分類選擇還要靠它們），用完還原。
`document_closure/` 一行沒改。

分堆規則**跟陳核頁對稱**:承辦人在審核表「張貼」欄打 OK 才進「可貼」。
2026-08-11 承辦人的原話:「張貼要和陳核一樣，是我逐筆勾選，不能直接就跑」。
`tests/test_post_web_batch.py::test_unchecked_is_never_posted` 釘住這條。

已經有的關卡:`--go` 必須帶 `--expect`（不帶直接拒絕）、動手前自己再算一次、
逐筆失敗即中止、已公告過的不重貼、缺 `sssh_publish_unit` 就不開 Chrome、
**文案裡有「待查核」網址一律擋下**（那可能是來文 PDF 夾帶的指令，貼上校網
等於幫它散布 —— 公告頁只標紅提醒，到張貼這一步必須擋）、
**收文那個 Chrome 還開著就不開 Chrome**（2026-08-12 新增，坑 #22）。

預覽（含 `--json`）也一起印**實際會傳哪幾個附件** —— 附件檔名會出現在校網上，
家長看到的就是那幾個名字，所以要能事前核對。判定不另立標準:承辦人在摘要頁勾過
的話 `_find_attachments` 自己會讀那份「附件選擇.json」，這裡只是把它算出來的答案
印出來。只在其他關卡都過了才算（那支會走整個目錄還讀檔算雜湊）。

**版型**:區塊標題改**靠左、字級 1.15em → 1.3em**（承辦人 8/11 看過實物後定的）。
一樣是換 `sssh_style._S["h3"]` 這個模組屬性、用完還原 —— `sssh_style.py` 在
`document_closure/` 底下，系管師那條路也在用它，**預設值一個字都不能動**。

### 張貼頁（2026-08-12 做完，`ui.web_payload` ＋ `/api/web/*`）

照**陳核頁**的形狀:三堆（會貼／勾了但貼不出去／其他待辦）＋二段確認，
判定全吃 `post_web_batch.plan()`，送出走 subprocess `--go --expect=…`。
**沒照存查頁**（那頁「順序是資訊」是 `process_document_closure` 只做第一筆造成的，
張貼逐筆各自獨立）；**也沒照公告頁那個綠底**（綠底的意思是「這一頁不會送出東西」，
張貼正好相反 —— 這是全站唯一真的對外的動作，用紅底）。

跟陳核頁三個不一樣的地方:

1. **核對的東西不同**。陳核核對兩行字、存查核對一個 8 位檔號，張貼核對的是
   **全校師生家長會讀到的那幾百字**。所以「會貼」那幾筆整篇攤開（`.wbody`，
   正常字體、行距 1.9、超過 290px 自己捲），上面另一個等寬框列**標題／分類／附件**
   —— 那三樣是填進校網表單的欄位。確認對話框裡**一樣列全文**，不是只有標題。
2. **Chrome 的燈條件相反**（坑 #22）。用的是 `post_web_batch.chrome_in_the_way()`，
   **不是** `ui.chrome_state()`。定義只寫在執行器那一處，那支動手前也自己再檢查
   一次。兩盞燈長得像但意思相反，千萬不要合成一支。
3. **警告會擋送出**。`plan()` 的警告（目前只有缺 `sssh_publish_unit`）沒清掉，
   送出鈕就是灰的 —— 跑下去會在「發布單位」那步停住，而那時 Chrome 已經開了、
   校網也登入了。

`/api/web/start` 五道關卡（順序就是這個順序，測試按這個順序鋪設）:
沒帶「確認」不動；畫面看到的那幾筆要跟現算的一樣；**那幾筆的內容也要一樣**
（比 `fingerprint()`:標題＋內文＋分類＋附件，沒帶指紋也不放行）；
`plan()` 有警告不放行；`chrome_in_the_way()` 有話說不放行。
帶下去的 `--expect` 是 `文號:指紋` —— 那支動手前自己再算一次比對。
浮窗走 `--job=web`（`prep_hud` 的 `RUN_LABEL`/`DONE_LABEL` 要有這個 key，否則
會**退回 prep**、盯著收文那支的舊訊息）。這一段**不碰 edoc、不用插卡、螢幕鎖不鎖
無所謂**，所以浮窗與頁面只寫「不要動滑鼠」（它自己開的 Chrome 正在填表單），
不寫憑證與螢幕鎖 —— 假警告會讓真警告一起被當成裝飾（坑 #13）。
`tests/test_ui_web.py` 釘住這幾道關卡（15 條）。

### 第一次實跑（2026-08-12，1 筆，成功）

`MWAA1156007964`。**只勾 1 筆**試水溫是對的做法，會貼超過 1 筆時頁面自己會寫
這句提醒。當天核對的結果:

- 置頂出現在第一筆;標題、五個內容區塊、報名網址都正確
- 發布日期 2026/08/12、下架 2027/05/25
- 沒有誤傳附件，也沒有重複標題
- 本地 `*已公告.txt`、CSV 清冊、審核表、校網公開頁**四邊一致**
- ⚠️ **發布單位是承辦人手動改的**（程式跑到那步是「圖書館」）—— 見待補

**開跑前先把收文那個 Chrome 關掉**（坑 #22）。頁面會擋，但知道原因比較不會慌:
張貼不需要 edoc、不需要插卡，它自己開一個新的 Chrome 登校網。

### 貼到哪個板:沒有「資媒組的板」（2026-08-12 查證，別再查一次）

`ANNO_URL` 寫死圖書館頁的「圖書館公告」模組，看起來像個該改成資媒組的東西 ——
**但資媒組那頁（`/nss/s/main/p/info`）根本沒有公告模組**，只有沿革、聯絡資訊、
人員配置。校網上沒有資媒組的板可以貼。
所以「貼在圖書館公告、發布單位掛資訊媒體組」就是正確狀態，
`ANNO_URL` **不需要改**（也就不用為它做覆寫）。首頁與圖書館公告清單上那個
「發布單位」下拉是篩選器，資媒組的東西靠那一欄被篩出來。

## 安裝與交接（2026-08-12 新增）

給接手的人的入口是 **`安裝說明.md`**（那份是寫給不寫程式的人看的），流程只有兩步:
點 `安裝.bat` → 點 `啟動.bat` → 到「設定」頁填自己的 PIN 與校網帳密。

| 檔 | 做什麼 |
|---|---|
| `安裝.bat` | **只有 ASCII**。找 Python → 交給 `install.py` |
| `install.py` | 記下這支 Python、裝套件、複製 `env.env`、**放桌面捷徑**、跑 `doctor.py` |
| `啟動.bat` | 用 `python-path.txt` 記的那支跑 `ui.py` |
| `doctor.py` | 環境檢查:Python／套件／Chrome／HiCOS／`env.env` 必填鍵／規格檔／審核表 |
| `requirements.txt` | `openpyxl`（**README 舊版沒列**）／selenium／pyyaml／pywin32／pillow／pyautogui |
| `handover.py` | **打包成一個 zip ＋ 一個自解 exe 交給別人**（桌面產出）|

### 交接包（`handover.py`）

「安裝檔」= 一個 zip，不是單獨一個 bat。**單獨複製 `安裝.bat` 過去不會動**
（實測只吐 `can't open file install.py`，對非工程同仁完全看不懂）——
所以 `安裝.bat` 現在會先檢查 `install.py`／`ui.py` 在不在，不在就用英文講清楚
「要整個資料夾」。

打包的判斷**不自己另立一套**:用 `git ls-files --cached --others --exclude-standard`
（＝ `.gitignore` 說可以公開的東西）。理由是那份清單早就為了「這個 repo 是 public」
寫得很嚴，而且兩邊都在維護它;自己再寫一份遲早分岔。
git 說可以之後**還要再過一次自己的黑名單**，而且**打包完把 zip 打開再檢查一次**:

- 混進 `env.env`／`*.xlsx`／公文目錄 → 刪掉那個 zip
- **少了必要的檔**（`安裝.bat`／`啟動.bat`／`安裝說明.md`／`ui.py`…）→ 也刪掉

**自解 exe** 用的是 7-Zip 的 `7z.sfx`（純解壓縮），**刻意不用**那種「解到暫存
資料夾再自己跑起來」的安裝式模組 —— 這套工具的資料夾要長期留著（`env.env`、
下載的公文都在裡面），解到暫存跑完就沒了正好是最糟的。
沒裝 7-Zip 就只產 zip，不會失敗。
⚠️ 驗 exe 的內容要驗**壓進去的那份暫存實物**，不要去解析 `7z l` 的畫面輸出:
7z 走 OEM 字碼頁，中文路徑用 UTF-8 解會變亂碼 → 每個檔都被判成「少了」
（2026-08-12 第一版就是這樣自己騙自己）。

⚠️ **Python 是唯一可能沒有的東西**（承辦人 2026-08-12 的觀察）。所以
`安裝.bat` 找不到 Python 時會**問要不要用 winget 幫他裝**
（`winget install Python.Python.3.13 --scope user`，不需要管理員）。
兩個刻意的設計:**一定先問**（不在別人電腦上不問就裝東西）、**預設 N**
（這個視窗可能是被自動化叫起來的，空白答案不可以觸發安裝）。
裝完要**關掉視窗重開**才吃得到新的 PATH，訊息有講。

⚠️ 那第二道不是多餘的。第一版打出來的 zip **少了三個中文檔名的檔**
（`安裝.bat`、`啟動.bat`、`安裝說明.md` —— 剛好是對方最需要的三個），
原因是 `git ls-files` 預設把非 ASCII 檔名轉義成 `"\345\256\211..."`，
`os.path.isfile()` 對不到就**默默跳過**。修法是 `-c core.quotepath=false`，
**那個參數不能拿掉**。

### 「有填」不等於「填了自己的」（2026-08-12）

安裝流程會把 `env_example.env` 複製成 `env.env`，而那份範例是**有值的**
（`pin=000000`、`sssh_account=abcdefg12345`）。所以只檢查「有沒有填」會回報
**✅ 已填**，接手的人以為好了，要到收文插卡那一刻才發現 PIN 是錯的。
`env_config.placeholders()` 認出「跟範例檔一模一樣」的值，`doctor` 把它算成 ❌。

只認 `MUST_BE_YOURS`（PIN／校網帳密／發布者／發布單位）那五個 —— 摘要順序與模型
設定的範例值就是**出廠預設**，本來就該照用，一起報成「還沒填」是假警告
（坑 #13:假警告會讓真警告一起被當成裝飾）。

⚠️ **`.bat` 裡絕對不能放中文 —— 連註解、連檔名都不行。** cmd 讀 `.bat` 用的是
**系統 ANSI 字碼頁**（這台是 Big5/cp950），不是 UTF-8。踩過兩次:

- 2026-08-12:`安裝.bat` 放中文 → `@echo off` 被吃成 `cho`，整支跑不動。
- 2026-08-14:`啟動.bat` 只是**在註解與錯誤訊息裡提到 `安裝.bat` 這個中文檔名**
  → 承辦人點桌面捷徑**完全沒反應**。當時「測過了」是假的:測的是另外抄的
  ASCII 副本，**沒有真的跑過那個檔本身**。

⚠️ **`requirements.txt` 也不能放中文**（2026-08-14，同事那台炸出來的）:
**pip 用 `locale.getpreferredencoding()` 解這個檔**，不是 UTF-8。同事那台
（pip 24.2）直接
`UnicodeDecodeError: 'cp950' codec can't decode byte 0xe8` 整個安裝中止，
而這台（pip 26.1）好好的 —— **典型的「只在別人機器上壞」**。
而且 traceback 全是 pip 的內部檔案，看起來完全不像我們的問題。

所以中文一律由 Python 印（`install.py`、`doctor.py`、`ui.py` 自己講），
`.bat` 與 `requirements.txt` 只留 ASCII。兩道護欄:
`tests/test_installer.py` **直接讀檔案位元組**，有非 ASCII 就紅;
`handover.py` 打包前再檢查一次，有問題**整個不打包**（寧可包不出來，
也不要寄一個對方點不動的東西）。

**教訓（今天學了兩次）:**
1. **驗一個檔，就要跑那個檔，不要跑它的副本。**
2. **「會被別的程式用系統語系編碼讀」的檔案，一律純 ASCII。** 這種錯在自己
   機器上看不出來 —— 要別人的機器、別的版本才會炸。

⚠️ **點兩次捷徑**:`ui.main()` 會先連連看 `127.0.0.1:8760`，已經有人在服務就
**只把那一頁打開、不再起一個服務**（結束碼 0，不讓 `啟動.bat` 跳紅字）。
不能靠「bind 失敗」判斷 —— `HTTPServer.allow_reuse_address = 1`，而 Windows 的
SO_REUSEADDR 允許**兩個 socket 綁同一個埠**（跟 Linux 相反），實測第二支會默默
起來，兩個服務搶同一個埠。

⚠️ **坑 #4 現在被釘死了**:`install.py` 把「跑它的那支 Python」寫進
`python-path.txt`（已 gitignore），`啟動.bat` 用同一支。而 `ui.py` 是用
`sys.executable` 起所有子程式，所以整條流程一定同一支。實測這台:
`python` → `C:\Python314`（**有** openpyxl）、`py` → 3.14.4（**沒有**）。
`doctor.py` 檢查的是**跑它的那一支**，檢查別支等於沒檢查。

`doctor.py` 的輸出設計成**可以整段複製給別人看**:只講「有沒有填」，
**不印任何密鑰**（`tests/test_doctor.py` 第一條就在釘這件事）。

**不用 VS Code、不用會寫程式**（2026-08-14 承辦人問的）:點桌面捷徑（或
`啟動.bat`）→ 黑視窗 ＋ 瀏覽器自動開 `127.0.0.1:8760`。兩個刻意的決定:
- **黑視窗不藏起來**。技術上用 `pythonw` 就沒有視窗，但收文／陳核／存查／張貼的
  進度與錯誤都印在那裡 —— 藏起來等於出事時什麼線索都沒有（8/14 查那個假標記
  就是靠它）。改成在啟動訊息裡**明講「這個視窗就是程式本體，不要關掉」**，
  因為關掉之後網頁那頁會死，而畫面上不會有任何提示。
- 桌面捷徑用 PowerShell 的 `WScript.Shell` 建，**不靠 pywin32**（那一步跑在
  「套件裝好了沒」還不一定的位置），而且 best-effort:建不起來只警告，不讓安裝失敗。

## 下一段要做的事

1. **設定頁**（`ui_page.html` 最後一個 `soon`）。**分兩區，這是承辦人 2026-08-12
   定的原則:密鑰是個人的，工具只引導填寫、不代為保存或顯示。**

   | 區 | 內容 | 畫面怎麼處理 |
   |---|---|---|
   | **個人** | `pin`（自然人憑證，**輸錯會鎖卡**）、`sssh_account`／`sssh_password`、`google_ai_studio_api_key`、`anthropic_api_key` | 只顯示「已填／未填」。**值不回傳前端、不進 log、不進交接檔或截圖**;要改就是覆寫，不提供「看現值」 |
   | **共用** | `sssh_publish_unit`、`sssh_publisher`、`summarize_llm_order`、各 model 設定 | 正常顯示編輯 —— 這些是「這個學校、這個職務」的設定，換人接手照用 |

   順便放規格檔（`summarize_doc.md`／`announce_doc.md`／`routing_flags.yaml`）的入口
   —— 判斷都寫在那幾份裡，現在得自己去翻檔案。
   ⚠️ **這個 repo 是 public**。`env.env` 已被 `.gitignore` 擋掉，但畫面與 log 也要守
   上面那條，不然哪天貼交接檔或截圖就外流了。
   💡 `summarize_llm_order=claude` 那一棒走本機 `claude` CLI 的登入，**不需要任何
   key** —— 接手者最省事的預設（順便把沒資格的 `antigravity` 拿掉）。
2. **兩件事等別人看過**（2026-08-12 承辦人的話:「我自己覺得還行，但可能要再讓
   別人看看」、「我可能要讓系管師再看一次」）:
   - **設定頁的版面** —— UI 隨時可以改，資料格式不會跟著動（那一頁只是
     `env.env` 與兩份規格檔上面的一層皮），而那 33 條測試釘的是**行為**
     （密鑰不外流、存壞了不寫、YAML 驗過才存），改版面不會把護欄弄掉。
   - **安裝流程給系管師看** —— 重點是:`main.py`／`document_system.py`／
     `document_closure/` 一行沒改，新東西全都是新檔;`requirements.txt`
     補了 README 舊版沒列的 `openpyxl`。

**順手可做的小事**（不急）:

- `env.env` 的 `summarize_llm_order` 建議拿掉 `antigravity` —— 那個帳號沒資格
  （永久性錯誤），每份公文白等 10～13 秒才換棒（8/11 三份就白等 33 秒）。
  設定檔一行，不用改 Python:`summarize_llm_order=claude,anthropic`
- **跟系管師說 8 月版更那件事（坑 #26）** —— 存查／送發文前要先點【附件歸檔】，
  他的 `main.py 3` 沒有這段，會撞到一模一樣的牆（而且會寫出假的存查標記）。
  這件比雙因子更急，因為它讓「失敗」長得像「成功」。
- 跟系管師說一聲 edoc 的雙因子（坑 #20）—— 他的全自動路徑也會撞到。
  另外 `taipeion_login_selenium.py` 的 `--restore-last-session=false` 是筆誤:
  Chrome 那個 switch 只看有沒有旗標、不看值，寫成這樣等於**強制還原**，
  跟註解寫的本意相反。fork 側已用 `clear_stale_session()` 繞過，沒動他的檔案。
- `prep_hud` 現在整段都寫「請不要動滑鼠」，但 `prep_batch` 的**第二段可以動**。
  過度警告不會出事（只是讓人多等），但可以讓浮窗跟著階段換句話。

## 踩過的坑（別重犯）

1. **「OK」永遠不會變成「已辦」**。`upsert` 只填空白格，承辦人打的 `OK`（=請程式去做）
   在程式做完後不會被改寫。所以**判斷「做完了沒」不能只看審核表欄位**，要以磁碟痕跡
   （`*已陳核.txt` / `*已存查.txt` / `*已公告.txt` / 目錄在 `_closure`）為準。
   `review_sheet.is_archived` 與 `announce_doc._is_sent` 都踩過，已修。
2. **舊文有兩條進入路徑**：程式辦的（磁碟標記）＋ 承辦人自己宣告的（兩關都「已辦」）。
   手動在 edoc 辦掉的公文不會有任何標記檔，只能靠宣告。
3. **短文號不能直接餵 `--done`**。`upsert` 拿文號當 key，丟 `6621` 會**新增一列**假資料，
   不是更新原本那筆。先對回完整文號（`MWAA1156006621`）。
4. **`py` 不等於 `python`**。這台有兩個 3.14：`py`→3.14.4（**沒有 openpyxl**），
   `python`→`C:\Python314`（有）。**一律用 `python` 跑**，README 寫的 `py` 會讓審核表寫不進去。
5. **有催辦訊息要簽收時，收文會在簽收後停住**（2026-08-05 發現並修掉）。
   症狀：`main.py 4` 簽收完催辦通知就結束，沒下載、沒摘要。log 裡是
   `[WARN] 找到「承辦中」label 但無法 parse 數字` → `sidebar 承辦中 = -1`。
   根因：簽收催辦通知後 edoc 把「待辦公文」那組選單收起來（`height:0` +
   `overflow:hidden`，CSS `display` 仍是 `block`），Selenium 判定那些 `<a>`
   不可見、`element.text` 回空字串 → `_get_sidebar_paren_count` 讀不到 →
   `count > 0` 不成立 → 整段跳過。**8/3 測的時候會過，是因為那次沒有催辦訊息。**
   修法：`edoc_sidebar.py`（fork 新檔）用 `textContent` 讀數、用 JS `a.click()`
   點項目（會觸發 inline `onclick` 的 `_doGoTreeAction`，不需要元素可見）。
   `process_document_prep` 只在**原版讀不到時**才走這條，展開狀態行為完全不變。
   ⚠️ 收合時**連點擊都不能用原版**：`_click_sidebar_item` 最後一個 XPath 會退到
   第一個可見元素（收合時是 `<html>`），點下去等於沒點卻回報成功。
   共用的 `_get_sidebar_paren_count` / `_click_sidebar_item` 一行都沒動 ——
   全自動路徑（`main.py 1`/`2`/`3`）也在用它們。

6. **辦完的公文會留著「陳會 OK」，舊版會重送**（2026-08-04 發現並修掉）。
   坑 #1 的延伸：`upsert` 只填空白格，承辦人當初打的 `OK`（=請程式去送陳核）
   在送完、存查完之後不會被改寫，永遠停在 `OK`。而判「送過沒」只看 `*已陳核.txt`，
   手動陳核或早期流程辦的公文沒有那個標記檔 → **每次跑都算「可送」**。
   實測承辦人的審核表有 3 筆（MWAA1156007365 / 006895 / 007057）已存查甚至已公告，
   舊的 `post_draft_batch.py --go` 會把它們再送一次陳核，在 edoc 多開一輪簽核流程。
   已在 `evaluate()` 加 `rs.is_archived` 這一關擋掉。
   **教訓同坑 #1：任何「做完了沒」的判斷都不能只看審核表欄位。**

7. **送陳核之前，收文開的那個 Chrome 不能關**（2026-08-05）。
   `post_draft_batch` 是 attach 既有 session（`:9222`）而不是自己開瀏覽器，
   Chrome 關掉就 attach 不到，整批停在連線階段（**不會送出任何東西**）。
   ⚠️ 此時 `fill_in_draft` 會印「修補方式:跑 `python main.py 3` 重登 Chrome」——
   **那是結案存查**（歸檔完自動貼校網、無人工確認），照做會出事。
   那支是系管師的檔案不改，改成:①`ui.py` 的陳核頁在載入與送出前都檢查
   `:9222`，沒接上就把送出鈕停用並顯示中文說明；②`post_draft_batch` 在
   attach 失敗時補印一段正確指引蓋過去。正確做法一律是按「收新公文」。

8. **承辦中清單的 frame 不能用「簽收」按鈕去認**（2026-08-05）。
   `post_draft_batch.run` 原本借 `_switch_to_signoff_frame` 切 frame，那支是靠
   「頁面上有『簽收』按鈕」認的 —— 那是**待簽收清單**的特徵，承辦中清單沒有。
   結果第一筆就停在「切不到清單 frame」（0/5，**沒有送出任何東西**，逐筆失敗即
   中止那道關卡有正常運作）。改用同一支 generic 的
   `_switch_to_frame_with_xpath` + 承辦中清單的特徵 `//th[…'公文文號']`，
   與全自動路徑 `pending_doc` 同一條 xpath。
   另外 `post_draft_batch.focus_list` 切不到時會自己點左側「承辦中」把清單叫
   出來（用 `edoc_sidebar.click_sidebar`），不必要求使用者先手動切到對的頁。
   實測到「點開公文、開出閱覽器分頁」為止都通了。

9. **「有沒有在 edoc」不足以判斷 Chrome 停在對的地方**（2026-08-05）。
   公文閱覽器分頁（`/tcqb/oa/index.html?app=editor&doSno=…`）同樣是 edoc 網域，
   卻沒有 sidebar、沒有清單 frame。`attach` 抓到的當前分頁剛好是它時，
   原本那道「current_url 含 edoc.gov.taipei」的檢查照樣放行，整批停在第一筆
   （「左側選單找不到承辦中」）。
   而且**殘留的閱覽器分頁本身就會害送出誤判** —— `run()` 靠「window_handles 有沒有
   多一個」判斷公文開起來了沒，同一份的閱覽器已經開著時點清單不會再開新的。
   修法:`post_draft_batch.focus_main_window` 逐分頁找「有 #leftSideBar 的 edoc
   分頁」當主視窗；`viewer_tabs` 偵測殘留閱覽器，開跑前有就**停下來請人關掉**
   （不自動關 —— 裡面可能有手動填的東西）。`ui.chrome_state` 做同一組檢查，
   所以在按下送出**之前**就會擋住並說明。

10. **公文閱覽器第一次載入會整頁變純文字**（2026-08-06）。
    Selenium 點開閱覽器分頁時，常常拿到 `contentType = text/css` ——
    整份 HTML 被當成 CSS 檔丟出來（body 只有一個 `<pre>`、script 0 支、
    textarea 0 個），ExtJS 根本沒啟動。於是 `fill_in_draft` 找不到「我的意見」，
    停在 `[STOP] 填承辦文字失敗 — textarea 找不到`。
    **重新整理一次就正常**（text/html、47 支 script、3 個 textarea，
    `_find_visible_textarea` 抓得到 379×150 那個）。
    根因在伺服器/快取（那頁還帶著 Chrome 早就移除的 `appcache` manifest），
    不是這邊改得動的。`post_draft_batch.ensure_viewer_ready` 在呼叫
    `fill_in_draft` 前偵測，沒渲染就 reload（最多 2 次），還是不行就中止整批。
    `fill_in_draft` 是系管師的檔案，一行沒動。

11. **「有公文文號表頭」不等於「這是承辦中清單」**（2026-08-06，承辦人發現）。
    待結案清單、搜尋結果、催辦清單**都有**那個表頭。實測:sidebar 寫
    「承辦中(5)」，`dTreeContent` 裡卻是只有 1 筆的**待結案**清單（那筆狀態
    「陳核決行(待結案)」——昨天陳核完、主管決行後回到承辦人等存查的）。
    `focus_list` 只認表頭就通過，接著在那份清單裡找要送的公文，當然找不到，
    於是**停在那筆要存查的公文上**。承辦人的原話:「似乎你遇到要存查的文會卡住」。
    修法:`focus_list(driver, doc_no)` 改成**確認要送的那一筆真的在畫面上**，
    不在就點左側「承辦中」重叫完整清單；重叫後還是沒有就明說
    「可能已送走，或已不在承辦中（例如陳核完回來等存查）」。

12. **「有沒有多開一個分頁」不能拿來判斷公文開了沒**（2026-08-06）。
    edoc 有時會把公文**塞進既有的閱覽器分頁**（換掉內容）而不是開新的，
    這時 `window_handles` 數量不變。實測:一批 3 筆，前兩筆送出成功但分頁沒關，
    第 3 筆就被塞進舊分頁 → 停在「沒開出公文閱覽器分頁」（前 2 筆是真的送出去了，
    磁碟有 `已陳核.txt` 為證）。
    修法:`wait_viewer` 改認網址裡的 `doSno=<文號後 10 碼>`，新開或重用都認得；
    送出成功後 `close_viewer` 把那個分頁收掉，免得累積（也免得下次開跑被
    `viewer_tabs` 擋下來要人工清）。

13. **浮窗消失被當成「跑完了」的信號**（2026-08-06，承辦人回報）。
    原話:「小視窗不見了　我以為結束了我就碰了滑鼠」。送陳核那批的第 3 筆
    就是這樣壞的 —— 人在 Selenium 還在操作時動了滑鼠。
    根因是 `prep_hud` 舊版**連不上 ui.py 5 秒就自我關閉**，工作卻還在跑。
    修法:①連不上不再消失（警告，撐到 60 秒才收）；②結束時那句話改成
    **「可以動滑鼠了」**（成功、失敗都講），不是只寫「完成」；③結束時
    `winsound.MessageBeep` 響一聲 —— 浮窗會被 Chrome 的 pinCode 視窗蓋住，
    聲音蓋不掉；④每輪重設 `-topmost`（只設屬性，不 lift／不 focus，
    搶焦點會打斷 KdApp 那段）；⑤失敗不自動關，訊息留著。
    **教訓:不要讓人靠「有沒有視窗」去猜能不能動滑鼠 —— 要明講。**
    看長相:`python prep_hud.py --demo --job=send`

14. **edoc 會登出，而症狀長得像「找不到頁面」**（2026-08-06）。
    閒置一陣子後 Chrome 只剩 `index.jsp?logout=Y` 一個分頁。原本的訊息是
    「找不到公文系統主畫面」，看了不知道要幹嘛。`post_draft_batch.looks_logged_out`
    偵測登入頁網址，`ui.chrome_state` 與 `archive_batch` 都改成直接說
    「edoc 已經登出了，請按『收新公文』重新登入」。

15. **`focus_list` 不給文號時等於沒驗**（2026-08-10 讀 code 查出來，還沒踩到）。
    `archive_batch.pending_doc_nos` 原本呼叫 `focus_list(driver, label="待結案")`，
    但那支只在**有給 doc_no** 時才驗清單內容（`_list_has(driver, None)` 一律回
    True）。存查沒有特定文號可驗，於是只要畫面上停著任何有「公文文號」表頭的
    清單就直接採用 —— 而**送陳核跑完剛好停在「承辦中」清單**。那會把承辦中的
    公文當成待結案印給人看。坑 #11 是同一個洞的反方向。
    修法:存查這條路**不採用畫面上現成的清單**，一律先 `click_sidebar("待結案")`
    重叫；再拿 sidebar 的「待結案(N)」交叉核對筆數（讀到的比 N 多 = 根本不是
    這份清單，直接拒絕；比 N 少多半是分頁，照樣給看但講明白）。
    **教訓:通用函式的「可選參數」不給，往往等於把那道檢查關掉。**

16. **`已存查.txt` 攔不住重複簽章**（2026-08-10）。
    `archive_batch.evaluate` 看到結案目錄有 `*已存查.txt` 會判「已經存查過」，
    但 `process_document_closure` **根本不看那個標記檔** —— 它照樣把那份公文
    當一般待結案公文再送一次「確定存檔」簽章。也就是說預覽寫「擋下」、實際
    會做，正是最壞的分岔方向（重複簽章 = 2026-07-16 事故）。
    真正會發生的情境:歸檔成功寫了標記，但 edoc 那邊清單沒更新。
    修法:那支是系管師的檔案不改，所以閘門長在 fork 側 ——
    `plan()` 把這種標成 `danger`，**整批不跑**，請人先去 edoc 確認。

17. **殘留閱覽器分頁的檢查只認 `app=editor`，存查那種是 `app=check`**
    （2026-08-10，存查第一次實跑就踩到）。
    症狀:承辦人回報「停了」。log 上 `點到待結案最上方公文「MWAA1156007696」`
    下一行卻是 `切到公文閱覽器分頁，URL=…app=check&doSno=1156007710` ——
    **點的是 7696，切過去的閱覽器是 7710**。接著在那份錯的公文上判「如擬」、
    按下載，匯出對話框 10 秒沒關 → 第 1 輪失敗中止。
    （逐筆失敗即中止有正常運作:`document_download_closure` 沒有新目錄、
    沒有新的 `已存查.txt`，**一份都沒歸檔**。）
    根因:7710 的閱覽器分頁是 8/6 送陳核留下來的殘留，而
    `ui.chrome_state` 與 `post_draft_batch._VIEWER_MARK` 都寫死 `app=editor`
    —— 那是**送陳核**的閱覽器。存查開的是 `app=check`，按下載那步還會多一個
    `app=genpages`。於是那道「開跑前有殘留就擋下」的關卡對存查形同虛設。
    修法:`_VIEWER_MARK` 放寬成 `oa/index.html?app=`（三種都涵蓋）；
    另外兩個用到它的地方（`wait_viewer`／`close_viewer`）都**同時**要求
    `doSno=<這份公文>`，認得更寬只會認到同一份，所以放寬是安全的。
    `archive_batch` 開跑前也自己檢查一次殘留分頁（CLI 直接跑也擋得到）。
    **教訓:同一個「特徵字串」被兩條路徑共用時，先確認另一條路的長相真的一樣**
    —— 這是坑 #2「改判定基準前先查誰在用」的鏡像:不是放寬害了別人，
    是別人的定義本來就不適用我這條路。

19. **「操作時間逾期」的警告視窗會冒充公文系統主畫面**（2026-08-10，承辦人回報）。
    症狀:收文時一直跳出灰色小視窗「操作時間逾期，請您重新登入，謝謝！」，
    而備料跑到後面幾筆變成 `切不到清單 frame,跳過`，最後印「共處理 4/6 筆」、
    結束碼 0 —— **看起來跟正常結束一模一樣**，實際少做兩筆。
    兩層根因:

    ① **閒置的是 LLM 那段**。下載一份只要 12 秒，摘要卻要 1～2.5 分鐘，
    那段時間 Selenium 完全沒有碰 edoc。實測 8/10:進承辦中清單到第一次失敗
    中間 4 分 50 秒，其中 **3 分 42 秒在等 AI**。而且 `summarize_llm_order`
    第一棒 `antigravity` 每份還要先耗 11～13 秒才拿到「帳號沒資格」換棒
    （永久性錯誤卻每份都重試）。**公文越多越後面越掛** —— 那天 6 筆裡有
    2 筆已有總結秒過、只有 2 筆真跑 LLM 就已經被踢，六筆全要摘要會從第 3 筆
    開始全掛。

    ② 那個視窗是 edoc `window.open` 開的**獨立視窗**，不是 JS alert
    （`taipeion_login_selenium` 那段「自動接受所有 JS dialog」按不掉它，
    所以它會一直堆著），網址是
    `https://edoc.gov.taipei/tcqb/home/sessionTimeout.jsp` ——
    **住在 `/tcqb/home/` 底下，跟公文系統主畫面同一個路徑**。
    於是 `ui.chrome_state` 那兩道 `any("/tcqb/home/" in u)` 被它冒充過去。
    實測承辦人當下的 Chrome:`looks_logged_out=True`，
    `chrome_state()` 卻回 `{'ok': True}` ——
    **陳核頁與存查頁共用這盞燈，兩頁都亮綠燈、送出鈕可以按。**
    這是最壞的方向:畫面說可以、實際已經被踢出去了。

    修法（②已修）:`post_draft_batch` 新增 `looks_timed_out()` 與 `has_home()`
    （後者明確排除 sessionTimeout.jsp），`chrome_state` 改吃這兩支，且**逾期
    檢查排在最前面**（它是確診，其他訊號都只是推測；訊息要多一句「先關掉那個
    小視窗」，不然重新登入後它還杵在那裡繼續騙後面的檢查）。
    存查那條路沒中招 —— `archive_batch` 是靠「有沒有 `#leftSideBar`」認主畫面，
    不是靠網址字串。`tests/test_ui_send.py` 有回歸測試（含「主畫面看起來還在」
    那個更嚴苛的情境）。
    教訓同坑 #17:同一個特徵字串被兩條路徑共用時，先確認另一條路的長相真的一樣。

    **① 也修掉了（同日）**:新的 `prep_batch.py` 把下載與摘要拆成兩段 ——
    見下面「收新公文怎麼走」。`main.py 4` 一行沒改，**所以那條路還是會被踢**，
    UI 的「收新公文」已改指向 `prep_batch.py`。

20. **edoc 改採雙因子:進 edoc 之後還要再輸入一次卡片 PinCode**
    （2026-08-11，卡了整個上午、連跑六次一份都收不到）。
    edoc 登入頁自己寫著:「為配合政府資安政策及提升系統安全性，**自 114 年 6 月
    17 日起採雙因子身分驗證**」。也就是說 TAIPEION 用自然人憑證登入只是第一關，
    點「公文(學校)」進 edoc 之後，**edoc 會再要一次卡片 PinCode**。

    症狀非常會騙人:
    - TAIPEION 那段全部正常（log 還讀得到「公文(學校) 待辦數 = 6」）
    - 進 edoc 後停在 `index.jsp` **一動也不動**（等 31 秒網址完全沒變）
    - 同時彈出「操作時間逾期，請您重新登入」的獨立視窗
      → 於是所有人（包括我）都往「session 逾期」的方向查

    ⚠️ **那個逾期視窗有兩個完全不同的成因**，別再混在一起:
    ① 真的閒置逾期（坑 #19，8/10 那次:等 AI 等太久被踢）
    ② **雙因子第二關沒過**（本坑，8/11:根本還沒登入成功）
    症狀一樣、修法完全不同。分辨方法:把逾期視窗關掉之後，底下那頁如果是
    **「憑證登入 / 請輸入卡片 PinCode」**，那就是②。

    修法（fork 側，`prep_batch.py`）:偵測到停在 `index.jsp` 且畫面上有 PinCode
    欄位 → 用 `env.env` 的 `pin=` 補填一次並按登入。
    ⚠️ **只填一次，失敗一律不重試** —— 自然人憑證連續輸錯 PIN 會鎖卡，要跑
    戶政事務所解卡，代價遠大於少收一次文。欄位選擇器寫得很窄（只認
    `pinCode`，**不吃** `input[type=password]` 那種寬鬆條件），寧可找不到也不
    亂填;按不到登入鈕就把 PIN 留在框裡讓人自己按。呼叫端絕不可放進重試迴圈。

    **系管師那條路也會撞到** —— `main.py 1`/`2`/`3` 同樣是 TAIPEION 登入完就
    直接用，沒有處理第二關。8/11 實測 `main.py 4` 撞在一模一樣的地方。
    值得跟他說一聲。

21. **登入卡住時，可以接既有的 Chrome 直接收文**（2026-08-11）。
    `python prep_batch.py --attach` 會接上**已經開著、已經登入好**的 Chrome，
    跳過整段登入。送陳核與存查本來就是這樣做的，備料沒理由一定要自己重登。
    8/11 就是靠這條路把公文收下來的（承辦人手動過了 PinCode 那關之後）。
    這條路**不會 taskkill Chrome、不會清 session** —— 那個 Chrome 就是本錢。

22. **張貼跟陳核／存查對 Chrome 的要求正好相反**（2026-08-12 做張貼頁時讀 code
    查出來，**還沒踩到**，已擋掉）。
    陳核與存查是 attach 進「收文開好、已經登入」的那個 Chrome，所以那兩頁的燈
    是「它**開著**才給按」（`ui.chrome_state`）。張貼**完全不碰 edoc**，是自己開
    一個新的（`post_web_review._launch_bare_chrome`）—— 而兩邊都走
    `taipeion_login_selenium._build_chrome_options()`，用的是**同一個
    `--user-data-dir=…\Chrome-Selenium`＋同一個 `--remote-debugging-port=9222`**。
    同一個設定檔被兩個 Chrome 佔住時只有兩種下場，都不是我們要的:

    ① 新開的 chrome.exe 把命令交給既有實例就自己退場 → chromedriver 接到的是
    **收文那個 Chrome**，於是把 edoc 那個視窗開去校網，收尾的 `driver.quit()`
    還會把它整個關掉 —— 而那個 session 要插卡、輸 PIN、再過雙因子才回得來（坑 #20
    卡了整個上午）。
    ② 或者 chromedriver 等不到瀏覽器，直接拋 WebDriverException（一片英文）。

    **原作者那條路不會撞到**:`main.py` 一被 import 就 `_close_selenium_chrome_only()`
    把 Selenium Chrome 殺掉，才輪到 `main.py 5` 開乾淨的。fork 這側**故意不照做**
    —— 那個 Chrome 裡有插卡登入好的 edoc session，不能說殺就殺。
    修法:`post_web_batch.chrome_in_the_way()`（讀 :9222 的分頁清單，連得上就是還
    開著），張貼頁的燈與 `/api/web/start` 都吃它，那支開 Chrome 前也自己再檢查一次。
    **兩盞燈不可以合成一支** —— 條件相反，合了一定有一頁是錯的。
    教訓同坑 #17／#19，但這次是**反過來**:不是特徵字串共用，是「同一個資源，
    一條路要它在、另一條路要它不在」。`tests/test_ui_web.py` 有回歸測試。

23. **失敗卻用結束碼 0 離開 = 畫面替你宣告成功**（2026-08-12 審出來，已修）。
    UI 判成敗只看結束碼:`tick()` 看到 0 就寫「張貼結束」、報成功、**3 秒後自動
    收起面板**。`post_web_batch` 原本不管貼了幾筆都正常 return，於是「一筆都沒貼」
    跟「全部貼完」在畫面上長得一模一樣 —— 而這是唯一對外的動作。
    陳核（`post_draft_batch`）與存查（`archive_batch`）失敗都 `SystemExit(1)`，
    只有這支漏了。修法:`ok != total` 或 `total == 0` 就 `SystemExit(1)`。
    **以後每新增一支 Job，這件事要一起檢查** —— 面板會自己收起來，
    而「面板收起來」已經被承辦人讀成「跑完了、可以動滑鼠了」一次（坑 #13）。

24. **只比「鍵」不比「內容」= 確認過的字可以被換掉**（2026-08-12 審出來，已修）。
    送出前的比對原本只看文號:文號一樣就放行。但承辦人在確認框裡看的是**那幾百字**
    （標題、內文、分類、附件），不是那 14 個字元 —— 中間有人在 Excel 動了「公告」欄、
    或另一邊剛跑完產文案，貼出去的就不是他看過的東西，而清單比對完全沒感覺。
    修法:`post_web_batch.fingerprint()` 把標題＋內文＋分類＋附件壓成 10 碼，
    `_slim` 帶給畫面，按確定時帶回來比;`--expect` 改成 `文號:指紋`，
    執行器動手前自己再算一次。**沒帶指紋一律不放行**（那代表畫面是舊版本）。
    教訓:「畫面看到的跟現算的一樣」這句話裡的「一樣」，要指到人真正看的那個東西。
    陳核頁與存查頁值得回頭想一遍 —— 陳核比的是文號（實際送出的字在
    `送出文字` 裡，會跟著擬辦變）、存查比的是文號與順序（實際寫進去的是檔號）。
    兩邊都還沒改，但同一個洞的形狀在那裡。

25. **「發布單位」回報選好了、實際沒設進去 → 公告被掛成圖書館**
    （2026-08-12 實跑，已修）。詳細的來龍去脈與那個欄位的長相寫在待補第一條。
    這裡只留最會重犯的那句:**那一欄不是 `<select>` 而是 `input[id^="ct-etAnnoGroup-"]`，
    旁邊 hidden 才是真正送出去的群組 id —— 光把文字塞進 input 沒有用。**
    而「沒選單位」的公告不是留空，是被校網算成**那一頁所屬的單位**（圖書館），
    所以症狀看起來像「它自己選了圖書館」，其實是「它什麼都沒選」。
    修法:`verified_publish_unit()` 設完讀回來確認 → 不對就重試原本那支 →
    還是不對就回 False 讓那一筆不發佈。

    ⚠️ 三個坑（#23／#24／#25）**同一天在同一支程式上抓到**，形狀完全一樣:
    **有人回報成功，但沒有人回頭確認。** 以後在這個 fork 裡看到「印了 OK 就往下走」，
    先問一句「這個 OK 是誰說的、有沒有人驗過」。

26. **edoc 8 月版更:存查前一定要先點【附件歸檔】** —— 沒點就按「確定存檔」會被
    提示視窗擋住，於是**簽章從沒開始，程式卻寫了「已存查」標記**，整批被自己的
    閘門鎖死（2026-08-14 實跑，當日修掉）。

    根因是市府的版更（承辦人找到手冊:「8月公文系統版更【附件資訊自動帶入附件
    編目欄位】操作手冊」）:

    > 過往操作是直接點選確定存檔或確定送發即可，**現修改為需先點選【附件歸檔】**，
    > 方能存查或發文，否則會跳出提示視窗。**若公文無附件，也需要先點選【附件歸檔】。**
    > 點【附件歸檔】→ 附件將自動帶入…**無須儲存，直接關閉此視窗即可** → 再點確定存檔。

    修法（fork 側，`archive_batch.attachment_archive_first()`）:換掉
    `document_closure._click_confirm_save_button` 這個模組屬性 —— 按「確定存檔」
    之前先點【附件歸檔】、把跳出來的視窗關掉（**只開再關**，附件是自動帶入的，
    要增修刪是人的判斷）。`document_closure.py` 一行沒改。
    - **做不到就回 False 不按確定存檔**。呼叫端是
      `if not _click_confirm_save_button(driver): return False`，那個位置在
      `last_signed_doc_no = doc_no` 與寫標記**之前** —— 所以不會簽章、不留假標記。
      寧可這一輪失敗，也不要再造一個「磁碟說辦完、edoc 說還在」的公文。
    - ⚠️ **切視窗會把 frame 重設到最上層**，而存查表單住在 frame 裡。關掉附件視窗
      之後一定要切回去（`_focus_frame_with`），不然接著找不到「確定存檔」。
    - ⚠️ **系管師那條路（`main.py 3`）沒有這段，一樣會撞到** —— 要跟他說。
    - `tests/test_archive_attach.py` 釘住這幾條（5 條）。

    ✅ **同日實跑驗證通過:5 筆全部歸檔完成**（10:14～10:33，後面四筆每筆間隔
    30 幾秒連著跑完）。跟早上那次的差別是**清單真的在縮短** —— 早上第 1 筆
    「成功」之後第 2 輪撞牆，正因為它根本沒歸檔。

    ── 以下是當時的症狀與後果，留著因為那個「假標記」的連鎖很值得記 ──

    症狀:歸檔第 1 筆 `MWAA1156007823` 走到「點確定存檔 → 等 pinCode」，log 印
    `[ERROR] 15s 內沒有偵測到 pinCode 視窗` —— **簽章從沒完成**。但下一步那道
    「文號從待結案**可見列**消失」的驗證，在**存查表單還開著、清單根本不可見**時
    必然成立 → 判成功 → 寫了 `已存查.txt`。第 2 輪重讀清單，待結案還是 5 筆、
    最上面還是同一份，系管師自己那道「可能沒真正生效」的護欄正確攔下並中止。

    後果是連鎖的:那個標記讓 `review_sheet.is_archived` 判它辦完（跑去舊文）、
    讓 `archive_batch.plan()` 判它 `danger`（坑 #16 的防重複簽章閘門）→
    **整批不跑**，而承辦人在介面上**沒有任何辦法解開**。

    ⚠️ **這一頁不能「跳過第一筆」**（承辦人當下的要求是「這邊應該要能勾選存查的」）
    —— `process_document_closure` 每輪只做待結案清單最上面那筆，不能指定，
    那是系管師的檔案不改。所以出路不是跳過，是**把那個說謊的標記清掉**，
    讓它變回一般待歸檔公文，整批就跑得動。
    做法:`/api/archive/clear-marker` ＋ danger 那一列的按鈕。三道關卡 ——
    要帶「確認」、**只准刪「還在剛讀到的待結案清單裡、而且狀態是 danger」的那幾筆**
    （不在清單裡的標記是正常的存查痕跡，刪掉會害那份公文再歸檔一次 = 2026-07-16
    事故）、路徑一律用後端 `evaluate()` 算的不吃畫面傳來的。刪完把快取清單作廢。

    **還沒做的**:`archive_batch` 跑完應該自己再讀一次清單、跟這一輪寫出來的標記
    對帳，當場講「那個標記是假的」。現在要等下一次讀清單才發現，而那段時間那份
    公文在介面上看起來已經辦完了。（同一個坑型:有人回報成功，沒人回頭確認。）

27. **公文超過 10 筆就分頁，第 2 頁以後從來沒被處理過**（2026-08-20 同事回報，
    當日修）。edoc 清單一頁只放 10 筆。中招的有兩條路:

    - **收新公文（備料）** —— `_collect_pending_doc_nos` 只讀「目前畫面上那張表」，
      也就是第 1 頁的 10 筆。第 11 筆之後一份都不會下載，而畫面印
      「承辦中清單共 10 筆」＋「共處理 10/10 筆」—— **跟全部收完長得一模一樣**。
      備料**不陳會**，公文會一直留在承辦中清單裡，所以筆數只會越積越多，
      一過 10 筆就開始漏，而且越積越嚴重。同一個坑型:有人回報成功，沒人回頭對帳。
    - **送陳核** —— `post_draft_batch.focus_list` 靠「這一筆在不在畫面上」找公文，
      第 2 頁的公文一律被判成「清單裡找不到 XXX —— **這份可能已經送走**」而跳過。
      訊息還是錯的:它沒走，只是在下一頁。

    修法（fork 側新檔 `list_pager.py`，`main.py` 那條路一行沒動）:

    1. **先試「每頁筆數」開到最大** —— 清單下方若有那個下拉（10／20／50…）就選
       最大值，全部公文併成一頁，後面完全不用翻頁，也不會有「處理到一半頁碼被
       打回第 1 頁」的問題。認定條件很嚴:選項要全是數字，而且**目前選的值要等於
       畫面上的筆數**（現在 10 筆、下拉也是 10 → 這才是每頁筆數，不是別的篩選器）。
    2. 沒有那個下拉才**點「下一頁」**，而且點完要**驗**:畫面上的公文號真的換了
       一批才算翻頁成功。按了沒動就停下，不重試也不假裝成功。
    3. 清單頁上同時有「送出／刪除／簽收／結案」這種按下去**不可復原**的按鈕，
       所以候選元素過三關才點:文字要完全等於翻頁字樣或目標頁碼、含黑名單字一律
       不碰、只點葉子元素（不點「包住下一頁字樣的那一整塊」）。
    4. **漏了要看得見**:翻完之後拿總數跟左側「承辦中(N)」對帳，少一筆就大聲喊
       「少了 N 筆」。這次的病就是漏收長得像正常結束。
    5. 每一筆動手之前先問「它現在在不在畫面上」（`ensure_page_has`）——
       點開公文、關掉閱覽器之後清單常被系統打回第 1 頁。

    ⚠️ **翻頁鈕的實際長相還沒在真機上確認過。** JS 用的是通吃寫法（文字／圖示檔名／
    onclick 的翻頁函式／頁碼連結四輪），對不對得上要看實機。清單超過 10 筆時跑:

        python list_pager.py        （唯讀，不點任何東西，把翻頁區印出來）

    印出來的 `<a>／onclick／src` 貼回來就能把選擇器釘死。若翻頁失敗，第 4 點那道
    對帳會喊「少了 N 筆」—— 不會安靜漏掉。

    ⚠️ **結案存查那條路沒動**（`archive_batch.pending_doc_nos`）。它本來就會印
    「清單可能分頁了」的警告，而且歸檔一筆、那筆就離開清單，第 2 頁自己會遞補上來。
    那一頁不可復原，不值得為了預覽多翻幾頁去點它。

    `tests/test_list_pager.py`（25 條）＋ `test_document_system.py` 三條
    ＋ `test_post_draft_batch.py` 一條釘住這些。

29. **「共處理 N/N 筆」是按「點過幾筆」算的，下載失敗照樣加一**
    （2026-09-03 實測）。`document_system` 的備料迴圈點完公文就
    `handle_opened_document(...)`，**不看它的回傳**，接著 `done += 1`。
    那天清單 2 筆、1 成功 1 失敗（失敗那筆是紙本轉線上），畫面照樣印
    「承辦中備料完成,共處理 2/2 筆」——**跟全部收完長得一模一樣**。
    跟坑 #27、坑 #23 同一型:有人回報成功，沒人回頭對帳。

    那支是系管師的檔，**不改**（改了兩邊行為會不一樣）。改在 fork 這側:
    `prep_batch.download_report()` 第一段跑完**看磁碟**逐筆對帳
    （目錄在不在、裡面有沒有來文主檔），老實再報一次。所以現在會看到兩句，
    **後面那句才是真的**。

    判「有沒有收到」用的是 `summarize_doc._MAIN_DOC_PATTERN`——跟後面判
    「能不能摘要」同一把尺。兩邊不同尺會出現「這裡說收到了、那裡說找不到主檔」。

    另外釘了一條測試:**線上簽核的公文收不到時，不准把責任推給紙本**。
    免得以後別的原因失敗，它一律喊「這是紙本」害人查錯方向。

18. **規格改動有時間差**。7/28–7/29 產的總結不含 7/30 才加的「判不出來寫 `待確認`」規則，
   所以舊總結會缺 `#存查分類:` 整行。重跑就正常，不是程式壞了。

## 待補（承辦人要查的）

- ✅ ~~**「發布單位」會回報成功但實際沒設進去**~~ —— **2026-08-12 當日修掉**
  （`verified_publish_unit`:設完讀回來確認、不對就重試原本那支、還是不對就
  **這一筆不發佈**）。下面留著來龍去脈，因為那個欄位的長相很反直覺:

  **那一欄不是 `<select>`，是 `input[id^="ct-etAnnoGroup-"]`**（後綴是亂數），
  沒選時的值是**「無群組」**;旁邊還有一個 hidden 欄位，那才是真正送出去的
  **群組 id**。所以:
  - `_select_publish_unit` 的策略 1（掃所有 `<select>`）**永遠不會中**，一律走
    fallback（點開下拉後在**整份 document** 找文字相符的元素點下去，只等 0.8 秒）
    —— 它回報的 OK 因此完全不可信。
  - **光把文字塞進那個 input 沒有用** —— hidden 的群組 id 塞不進去，送出去等於
    沒選單位。所以修法是「重試原本那支讓它自己去點」，不是自己塞值。
  - 沒選單位的公告會被校網掛成**該頁所屬單位＝圖書館**。這就是 8/12 第一批第 1 筆
    變成圖書館的完整因果（不是「預設值是圖書館」，是「沒選 → 算成頁面的單位」）。
  - 讀回來讀不到那個欄位（站台改版換 id）時**不擋**，只大聲警告 —— 擋下去的代價
    是整批停下，不能靠猜（坑 #13:假警告會讓真警告一起被當成裝飾）。

  <details><summary>當時的證據（三筆對照）</summary>
  `env.env` 是 `sssh_publish_unit=資訊媒體組`。當天貼了三篇，校網「單位」欄:

  | 標題（節錄） | 單位 | 備註 |
  |---|---|---|
  | 【課程】聽見臺灣… | 資訊媒體組 | 第一次實跑，**承辦人手動改的** |
  | 【講座】運用因材網… | **圖書館** ❌ | 第二批第 1 筆 |
  | 【培訓】2026上銀… | 資訊媒體組 ✅ | 第二批第 2 筆 |

  **兩筆的 log 都印 `OK:發布單位(自訂下拉)已選「資訊媒體組」**」，但第 1 筆實際
  是圖書館 —— **那個 OK 是假的**，這是最壞的方向（畫面／log 說做到了，其實沒有）。

  **同一批第 1 筆失敗、第 2 筆成功** → 一開始以為是時間差。
  </details>

  **2026-08-12 第二次實跑（4 筆）證明不只是時間差**:第 1 筆就被這道護欄擋下，
  重試三次都設不進去，**整批 0/4、結束碼 1、一筆都沒貼**（護欄如預期運作）。
  診斷讀到 `input[id^=ct-etAnnoGroup-]` 停在「圖書館」，而那一列的容器裡
  **另有一個文字是「資訊媒體組」的元素** —— 所以原本那支點到的根本不是下拉選項。
  它在**整份 document** 找文字相符的元素就點，於是:
  - 看不見的元素（下拉還沒 render）也算 —— 點了等於沒點，但它回報成功
  - 圖書館那頁側邊選單有 `<a href="info">資訊媒體組</a>` —— **點下去會離開表單**

  所以現在的做法是:原本那支跑完 → 讀回來 → 不對就**自己點**（點那個 input 開下拉，
  逐個「看得見、非導覽連結、像下拉選項的」候選點下去，**每點一次就讀回來驗**）。
  驗證是唯一的閘門 —— 候選挑錯只是白點一次，不會貼錯，所以候選寧可寬。
  ⚠️ 原本還多一道「hidden 群組 id 不可為空」，**已拿掉** —— 那是猜的（那一列附近
  不只一個 hidden），拿沒證實的東西當閘門會變成「明明選對了卻整批不給貼」。
  證實過的判斷只有一個:**那個 input 的值要等於要的單位**。

  ✅ **改完當天實跑驗證:4/4 全部貼出去，校網「單位」欄全部是資訊媒體組。**
  也就是說「自己點候選 + 每點一次就驗」這條路真的有效，不是只有擋人。

  怎麼查出來的（下次遇到同類問題可以照抄）:承辦人在**自己已登入的**分頁按「新增
  公告」把表單開著，在 DevTools Console 貼一段只讀的 JS，把「發布單位」那個 label
  往上四層的容器裡所有 input／select／葉節點 dump 成 JSON 貼回來。
  ⚠️ 用瀏覽器工具讀不到他那頁 —— 他的登入在**「學校」那個 Chrome 設定檔**，
  而擴充功能接的是另一個設定檔，另開的分頁一律是未登入狀態。
  校網帳密那邊早就有「設完讀回來確認」這招（`_force_field_value`，因為 Chrome
  自動填入會蓋掉帳密），發布單位這一欄漏了 —— 同一個形狀的洞。
  `tests/test_post_web_batch.py` 六條釘住這件事（含「文字對了但 hidden 是空的」
  也要擋、「讀不到欄位」不擋）。

  被貼錯的那篇（【講座】運用因材網…）**2026-08-12 由承辦人在校網上手動改回
  資訊媒體組**。程式不會回頭修已經貼出去的東西 —— 貼錯只能上校網改。

- ~~`MWAA1156007934`、`MWAA1156007936` 還沒備料~~ —— **2026-08-11 已補回**
  （連同新的 `MWAA1156007964`，共 3 份）。
- **9 個舊目錄缺總結**:`MWAA1156006169`、`006193`、`006221`、`006271`、
  `006289`、`006303`、`006304`、`006316`、`006489`。多半是早期跑到一半留下的
  （`document_download/` 裡還有幾個同名的 `.zip` 沒刪）。
  **收新公文不會去動它們**（見上面 `touched_since`）。要補得明確跑
  `python prep_batch.py --summary-only`，那會花 9 次 token、十幾分鐘 ——
  先確認這些公文是不是早就辦完了，辦完的就別花這個錢。

- **書展／藝文展覽**的存查分類。承辦人說學校檔號表有「**館際合作**」適用，但檔號未知。
  **不要猜**，猜錯就是公文歸錯檔。目前判成 `待確認`，`document_closure` 讀不到 8 位檔號會
  **安全停下**不硬填，所以擱著不會出事。實例：MWAA1156007544。
- `MWAA1156007051` 在 `document_download/` 與 `document_download_closure/` 各有一份總結，
  **分類不一致**（研習+研習資訊 vs 競賽+只有課外活動），貼校網會少一個分類。

## 環境與規矩

- 跑 UI：`python ui.py`（先關 Excel，不然審核表寫不進去）
- 跑備料：UI 按「收新公文」，或 `python prep_batch.py`。**會 taskkill 所有 Chrome**，先存好網頁。
  （`python main.py 4` 是原作者那條，還在但**會被逾期踢**，見坑 #19。）
- 讀卡機＋螢幕未鎖是硬性要求（鎖屏會擋 HiCOS）。
- git：`origin` = `mysunildc`（系管師，**唯讀，403 推不上去**）、`mine` = `sssh6002`（自己的 fork，推這裡）。
- **公文內容嚴禁進版控**。這個 repo 是 **public**。`.gitignore` 已擋 `env.env`、
  `document_download*/`、所有 `*.xlsx/*.xls/*.csv`。審核表放桌面。
- 判斷一律放規格檔（`summarize_doc.md` / `announce_doc.md` / `routing_flags.yaml`），
  不要寫進 Python — 承辦人改一行行為就變一片。
- **系管師那條路不改**：`main.py`、`document_system.py`、`document_closure/` 的既有行為。
  新東西長在 fork 這側。詳見 `CLAUDE.md`。
