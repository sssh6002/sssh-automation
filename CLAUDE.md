# sssh-automation — Claude Code 規則

## 對話輸出格式

對話的輸出完後，換行加上「引言區塊（Markdown `>` 語法，左側有色條）」，內容為粗體：

**輸出結束 LDC**
<!--**輸出結束 LDC 此輸出設定於 C:\Users\ldc\Documents\GitHub\sssh-automation\CLAUDE.md**
-->
## 操作規則

- 程式產生的截圖，確認不再使用後立即刪除
- 主動使用 superpowers plugin 中可用的 SKILL

## 施工規則

### 這是 fork，`main.py` 那條路是原作者的

本專案原始邏輯由系管師撰寫，承辦人 fork 出自己的版本。
`main.py`、`document_system.py`、`document_closure/` 的既有行為**一律不改**——
改了會讓兩邊同一支程式行為不同，之後合併或他自己跑時會出鬼故事。
新的防護長在 fork 這一側（`review_sheet.py`／`post_draft_batch.py`／
`announce_doc.py`），那幾支本來就有自己的確認關卡。
需要提醒後人的，只加註解、不動邏輯。

⚠️ 特別注意 `py main.py 3`（結案存查）歸檔後會**自動貼校網、無人工確認**。
2026-07-28 曾因此把一份他人業務的內部公文送上校網。此為原作者設計，不更動。

### 改判定基準前，先查誰在用

同一個判定常被多條路徑共用。2026-07-28 為了讓新句型被認出來而放寬
`ANNOUNCE_KEYWORDS`，結果同時讓 `main.py 3` 那條**全自動**路徑的閘門幾乎不再
攔任何東西——原作者設計的「不符關鍵字就停下來讓人看」失效了。
改共用常數前先 grep 一遍呼叫端，確認每條路徑的後果。

### 驗收要看被寫入的那一列

2026-07-28 花了很久查一個不存在的 bug：一直用 `--show <文號A>` 檢查結果，
但程式實際寫入的是文號 B（`--overwrite` 讓待處理清單的第一筆變成別人）。
畫面三次沒變，於是誤判成「正規化沒生效、模型沒切換」，往錯的方向查很久。

**動手改之前，先確認自己看的是不是程式真的動到的那一筆。**
不確定就把「實際被寫入的鍵」印出來，不要用自己以為的鍵去查。

### 判斷放規格檔，不要放 Python

擬辦、存查分類、公告口氣、業務歸屬這類判斷，一律放
`summarize_doc.md`／`announce_doc.md`／`routing_flags.yaml`。
承辦人改一行，行為就變一片；寫進 Python 他就改不動了。
Python 只做「唯一正解」的機械處理（格式正規化、逃逸、去重）。



